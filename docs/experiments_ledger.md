# Experiments Ledger — Closed & Failed Approaches

> **Tujuan:** mencegah agent mengulang pendekatan yang sudah gagal. Cek SEBELUM
> mulai eksperimen (step B0 workflow). Tambahkan entri di SETIAP akhir eksperimen
> (PASS atau FAIL).
>
> **Project goals:** efisien · robust · cepat · hemat token (cost/budget) ·
> scalable ke produksi.
>
> Jika sebuah hipotesis ada di sini, JANGAN dicoba ulang kecuali
> "re-try condition"-nya terpenuhi.

---

## Entries

| ID | Hipotesis | Hasil | Verdict | Re-try condition |
|---|---|---|---|---|
| **OCR-001** | PaddleOCR 3.7 / paddlepaddle 3.3 (PP-OCRv6, CPU) baca nomor lebih baik | 3.8GB RSS/cert, ~50s/cert, >6GB VA saat init → OOM di WSL 8GB. Kualitas teks bagus tapi tak bisa dipakai. | CLOSED — tidak viable | Hanya di GPU (VRAM offload) atau host ≥16GB RAM; revisit kalau produksi pindah dari WSL |
| **OCR-002** | paddleocr 2.9 + paddlepaddle 2.6 (CPU, stack stabil lama) lebih ringan & akurat | Korpus 74/74 ok, 7.1s/cert. Scan: organizer 18.4% (naik) tapi nomor **39.4%** (regresi vs 57.6 baseline), MACRO 39.8%. | CLOSED — gate FAIL (nomor) | Hanya kalau field-extractor diperbaiki menangani segmentasi paddle yang berbeda |
| **OCR-003** | EasyOCR CPU (max-side 960) — downscale biar cepat | 12.7s/cert. Scan: organizer **20.4%** (terbaik) tapi nomor **15.2%** (downscale merusak digit), MACRO 34.3%. | CLOSED — gate FAIL (nomor/dates) | Tidak ada di CPU @960; full-res 53s terlalu lambat |
| **OCR-004** | EasyOCR GPU (full-res) — VRAM offload + cepat | 9.7s/cert. Scan: organizer 16.3%, nomor **33.3%**, MACRO 39.3%. Bukan hasil terbaik. | CLOSED — tidak diadopsi (prioritas user = CPU) | Hanya kalau GPU jadi prioritas ATAU baseline nomor regresi |
| **LLM-001** | Prompt `g_evidence` (minta bukti singkat sebelum jawaban) | Tingkat turun (75.7% vs f_bias 82.4%). | CLOSED — regresi (v8 P3) | Perlu struktur output yang lebih ketat; replay setelah LLM lebih mampu follow structured format |
| **LLM-002** | Layout representation (markdown/annotation) dimasukkan ke prompt | Tingkat 77.0%/78.4% (< f_bias 82.4%); OCR-bound (25/74 berteks embedded). | CLOSED — ditolak (v8 P4) | Hanya kalau OCR kualitas naik drastis |
| **LLM-003** | Per-field LLM (A1) / full-text (A2 v2) untuk semua field | A1: tingkat 47.3%, 834 tok/cert, 125 calls. A2: MACRO 58.3% tapi tingkat 36.5%, 834 tok. | CLOSED — superseded oleh tingkat-only hybrid | Sudah digantikan pipeline v7/v8 (router + tingkat-only); tetap jadi referensi MACRO tinggi |
| **ORG-001** | Organizer normalization di `organizer_extractor_v2.py` (abbreviation map, strip institusi induk, trailing-context, merged tokens, signer EN roles) | Organizer exact **16.2% → 33.8%** (full LLM benchmark v8), fuzzy **59.5% → 82.4%**, tingkat **82.4% → 83.8%**, MACRO **55.2% → 58.9%**, 176 tok/cert, 29 calls. Full benchmark PASS. | **PASS** — semua gate met, no-regress | Sudah dipromosikan ke eksperimen; produksi butuh keputusan user. Efek samping: organizer_v2 bersih bisa menghilangkan signal router (`dept`/`luar`) → di-fix ROUTER-003 |
| **ROUTER-001** | Router expansion: naive `univ&!fak&!sem&!lomba→Univ`, `hima&!luar→Departemen`, `fak&!univ→Fakultas` | Precision turun ke 78.2% (12 salah) — rule naive terlalu agresif, `univ` fire pada "Universitas Airlangga" induk & `hima` fire pada raw-text. | **FAIL** — gate precision ≥95% tidak tercapai | Re-try hanya dengan signal yang diperketat (org-level, exclude BEM/hima-raw) |
| **ROUTER-002** | Router expansion data-driven: `bem_no_univ`, `bem+hima`, `sem+univ` (org-level signals) | Router coverage **36→42/74 @100% precision**, 0 salah. | **PASS** — coverage naik, precision ≥95% terjaga | Bagian dari pipeline v9 (dengan ROUTER-003) |
| **ROUTER-003** | Router `dept`/`luar` signal dari raw_text (OR dengan organizer) — fix efek samping organizer_v2 | Router coverage **42→45/74 @100%**, pull balik 3 cert (Ananda×2 dept+fak, NIC hima+luar) yang hilang signal-nya karena organizer_v2 bersih. Full benchmark: tingkat 83.8%, 29 calls. | **PASS** — coverage +3, precision tetap 100% | Dipertahankan; guard: `luar` dari raw_text terbatas daftar universitas eksternal (tidak menangkap fakultas internal) |
| **METRO-001** | Matcher evaluasi v1 `is_abbreviation_of` (threshold salah: hitung inisial-kata vs 0.5×panjang string) + fuzzy `token_overlap ≥0.5` | False positive ganda: `BEM FEB UNAIR` vs `BEM FKM UNAIR`/`BEM FEB UGM`/`BEM FEB UPNVJT` dikredit exact/fuzzy (0.67) padahal beda org. Organizer exact 33.8% ter-diskon (akronim valid tak pernah exact). | CLOSED — diganti matcher v2 (handoff v12) | Tidak ada — matcher v2 (subsequence huruf + rasio kata ≥0.3 + rasio huruf ≤0.6 + tolak parsial kontigu) |
| **METRO-002** | GT v8 inconsistency (organizer): `Kementriann` typo, `Akuntasi` typo, `FIT_Faiz Informatioon` typo, `NIC_Faiz` kurang spesifik | GT v9 dibuat: 5 organizer fixes (3 typo + NIC lebih spesifik + hakim_lomba Akuntasi). Tingkat tak tersentuh → 83.8% stabil. | CLOSED — GT v9 frozen, v8 tetap acuan historis | Tidak ada — GT v9 + matcher v2 = baseline evaluasi baru |
| **EXP5-001** | Multi-field LLM: tingkat + organizer dalam SATU call (variant C: LLM organizer hanya untuk cert organizer-miss pasca matcher v2, oracle GT = ceiling experiment) | Verbose: tingkat 83.8% ✅, organizer exact 39.2→41.9%, fuzzy 79.7→85.1%, MACRO 60.2→60.7%, 29 calls, tapi **token 195 > gate 176**. Compact: token 185 (masih >176) tapi **tingkat regress 81.1%** (−2.7pt). Gate token tak tercapai — cost intrinsik field kedua +10–19 tok/cert. | **FAIL** — GATE token (≤176) tidak tercapai; compact merusak tingkat | Tidak ada di token ≤176. Multi-field hanya layak bila budget token dilonggarkan (mis. ≤200) ATAU pindah ke model yang hemat prompt (API) |
| **OCR-005** | Analisis 3 OCR tool (saran user): DocTR / MMOCR / CnOCR — mana yang layak diuji sebagai pengganti RapidOCR+Tesseract (OCR sebelumnya ditutup: nomor gagal) | **CnOCR** = keluarga PP-OCR (ONNX) — SAMA dengan RapidOCR yang sudah di pipeline → redundant, tak menambah baru. **MMOCR** = PyPI terakhir Jul 2023 (stale ~3 tahun), butuh mmcv/mmdet stack berat, risiko dependency hell di WSL 8GB. **DocTR** = keluarga model baru (CRNN/SAR/ViTSTR/PARSeq), active (v1.0.1 Feb 2026), English/Latin out-of-box, layout-aware, ada varian MobileNet untuk CPU. | **DocTR = kandidat terbaik** untuk probe; CnOCR skip (redundant), MMOCR skip (stale + berat) | DocTR: probe terkontrol 5–10 cert scan dulu (fokus nomor/ tanggal, gate vs RapidOCR+Tesseract baseline); kalau nomor ≥59.6% baseline baru run penuh |
| **OCR-006** | DocTR probe (handoff v13 → v14): db_mobilenet_v3_large + crnn_mobilenet_v3_small (CPU, via `paddle_probe_safe --engine doctr`, RLIMIT_AS 8GB, watchdog RSS) pada 10 scan cert tetap | 10/10 ok, 0 watchdog kill, RSS ~1.3GB/cert, ~7.5s/cert (init ~3.2s). Like-for-like GT v9 + matcher v2 (10 stem sama): DocTR **nomor 14.3%** (1/7) vs baseline rapid_tess **28.6%** (2/7); dates **88.9%** (= baseline); organizer exact 10%→0% naik, fuzzy **70%** vs 60%. Raw check: `106/STF.E/HOLOGY7.0/x1t/2024` → DocTR `6/ST : E/ - L G 7. X1/2 o` (digit hancur, NIP/NIM juga drop digit). | **FAIL — gate nomor regresi** (14.3% < 28.6% like-for-like; baseline resmi scan 57.6%). Dates & organizer PASS; nomor = pola sama semua engine non-baseline. | Hanya layak sebagai **hybrid per-field**: DocTR utk dates+organizer, baseline RapidOCR+Tesseract tetap utk nomor (mis. merge/2-pass per field). Full replacement DocTR ditutup. |
| **OCR-007** | LFM2.5-VL-3B probe (VLM generatif, `tests/benchmark_lfm25_probe.py` + llama.cpp `llama-server` b10405): varian terendah RAM/quant = **GGUF Q4_0 (1520MB) + mmproj Q8_0 (556MB)**; transkripsi polos; 10 stem scan sama dgn OCR-006. Hipotesis: VLM ber-OCR (klaim "Better OCR" LiquidAI, support Bahasa Indonesia) bisa baca nomor/dates/organizer lebih baik dr engine klasik (OCR-001..006 semua gagal nomor) | 10/10 ok, 0 error, ~77s/cert (≈9x baseline 8.6s), server RSS ~4.9GB. Like-for-like GT v9 + matcher v2 (10 stem sama): **nomor 28.6% (= baseline — engine OCR pertama yang tidak regress**; paddle 39.4/easyocr 15.2/doctr 36.4 full-scan); dates **88.9% (=)**; **organizer exact 0→20%** (fuzzy 50→60%); MACRO 40.0→**44.4%**. Transkripsi bersih (FIT_Faiz NIP/NIM benar, Hology 7.0 utuh). nama_kegiatan 0% (= baseline; harness OCR-only). 0 LLM pipeline call. | **PASS (probe)** — semua gate met (nomor/dates no-regress, organizer +20pt, MACRO +4.4pt) | Full run 74 cert (~95 min) utk angka otoritatif; hybrid per-field LFM(organizer/dates)+rapid_tess(nomor) pola HYB-001 — LFM organizer +20pt potensi naikkan hybrid; cost 77s/cert+RSS 4.9GB = kendala produksi (F7 OCR queue); report `docs/report/lfm25_ocr_probe.md` |
| **HYB-001** | Hybrid OCR per-field: DocTR (dates+organizer) + baseline rapid_tess (nomor/activity/role), merge di level extracted-dict sebelum mapper, rule prioritas statis | Full 49 scan, GT v9 + matcher v2: baseline organizer 20.4%/61.2% → **hybrid 24.5%/65.3%** (DocTR 24.5%/63.3%); nomor **57.6% (= baseline**, DocTR 36.4%); dates **85.7% (= baseline)**; MACRO 45.8%→**46.8%**. 10-stem: nomor 28.6% (=), dates 88.9% (=), organizer 0/60%→10/70%, MACRO 40.0→42.2%. Embedded 25 (tanpa DocTR, fallback baseline): 50.5%/66.1% no-regress. Oracle = hybrid (rule merge sudah optimal). | **PASS** — semua gate met (nomor/dates no-regress, organizer naik, embedded no-regress), tanpa sentuh produksi | Produksi butuh keputusan user (integrasi `ocr_fallback.py` = 2 engine + merge field; atau tetap pipeline teks tunggal). DocTR runtime ~7.5s/cert + baseline ~8.6s = +~8s/cert tambahan |
| **NC-001** | Preprocessing nomor: anchor SEMANTIK (baris `NOMOR`/pola nomor via bbox RapidOCR) → crop region → re-OCR baseline rapid_tess, merge voting-disagreement (crop dipakai hanya bila ≠ baseline) — TANPA hardcode koordinat. Ver1: upscale digital crop render 3x (LANCZOS) → nomor 57.6% = baseline (0 gain). Ver2: **re-render region langsung dari PDF di zoom 6×** (clip+matrix, piksel nyata utk scan native dpi >216 — 33/49 scan) → **nomor 60.6% (+3.0pt)**, dates 85.7% =, organizer 20.4%/61.2% =, MACRO 46.3% (+0.5). Flip nyata: Falcon_Faiz `XI1/2024`→`XII/2024` (=GT) — re-render pulihkan numeral dari piksel nyata. anchor_miss 22/49 (rapid tak temukan baris nomor), runtime crop +5.2s/cert. | **PASS** — gate met (nomor 60.6% > 57.6%, no-regress field lain). Gain kecil tapi mekanisme tervalidasi (Falcon_Faiz membuktikan piksel nyata > interpolasi). | Integrasi produksi: disagreement (bukan lazy) di `ocr_fallback.py` via ENABLE_OCR_NUMBER_2PASS; **DEFAULT FALSE** — diukur lazy (nomor hilang) = **0/18 recovered** (18 cert nomor-hilang mayoritas memang tanpa nomor di GT); varian penuh ~+2-10s/cert. Rekomendasi: aktifkan hanya bila butuh +3pt nomor dgn cost tsb |
| **PROD-001** | Integrasi produksi: (1) promosi v9 — port organizer_v2 ORG-001 + tingkat_router ROUTER-002/003 dari tests/; (2) NC-001 2-pass nomor di ocr_fallback (flag OFF) | Pasca-promosi, re-eval GT v9 + matcher v2 (semua korpus): baseline rapid_tess scan **organizer 20.4→26.5%, MACRO 45.8→47.3%**; crop corpus nomor tetap **60.6%** (+3pt); hybrid 49-stem 46.8→**49.2%** (org 34.7%), embedded 52.3% no-regress. Pipeline smoke 1 cert: tingkat=Fakultas (router), organizer v9, nomor benar. pytest 31 passed. Trial easy/paddle re-eval naik (organizer_v2 v9), ledger OCR-002..004 tetap angka historis. | **PASS** — promosi v9 menghasilkan gain nyata di konteks produksi penuh, no-regress nomor/dates; NC-001 terintegrasi tapi off (lihat NC-001 re-try) | Rekomendasi selanjutnya: validasi varian region-OCR MURAH (tesseract psm tunggal, ~0.1s — saat ini psm7 gagal di region multi-baris) utk jadikan disagreement viable; kalau +3pt bertahan di ~1s/cert, flip flag ke true |
| **NC-002** | Region-OCR MURAH utk varian disagreement (jadikan NC-001 viable): ganti OCR region `rapid_tess` (3 config tess, ~5.2s/cert) ke engine tunggal — rapid / tess psm6 / tess psm13 / rapid+tess psm13 | Full 49 scan, GT v9 + matcher v2, like-for-like corpus_nomor_crop: control rapid_tess nomor **60.6%**; rapid **54.5%** (regress, 6.3s); tess_psm13 **57.6%** (= baseline, 4.3s); tess_psm6 **57.6%** (= baseline); rapid_tess_psm13 **54.5%** (regress, 6.2s). Tak ada varian ≥60.6%; multi-config tess (psm "",6,11) = satu-satunya yang recover (rapid_tess_psm13 = perilaku rapid-only → psm tunggal tak membantu). Latency total crop 4.3-6.3s = didominasi anchor full-page + region re-render (zoom 6×), BUKAN OCR region — floor ~4.3s. | **FAIL — gate nomor ≥60.6% tak tercapai (max 57.6% = baseline); latency ≤1.5s juga tak tercapai (floor ~4.3s)** | Region-OCR engine tunggal tak viable. Re-try hanya kalau region-OCR model tunggal bisa baca region multi-baris (DocTR sudah FAIL OCR-006 nomor 14.3%). Cost 2-pass = intrinsik ke multi-config tess; ENABLE_OCR_NUMBER_2PASS tetap default false |
| **STAT-001** | F0 roadmap v10: validasi statistik pipeline v9 (N=74) — 5-fold per-rule router precision (rule tetap, uji di fold) + bootstrap CI 1000× (resample cert, replacement) | Router recompute (organizer CURRENT tests/organizer_extractor_v2): **45/74 routed @100% precision**, konsisten klaim v9. Semua 13 rule min-fold-precision 100%. **8/13 rule LOW-N (fire <5)** → interval lebar, robustness tak bisa diklaim (lomba+org 15, dept+sem 5, tingkat_nasional 5 = satu-satunya fire ≥5). Bootstrap CI MACRO exact **56.0-64.4%** (mean 60.1%), fuzzy 70.3-78.3%; router coverage CI 50.0-71.6%. Temuan samping: `router_decisions.json` run v9 = snapshot organizer LAMA (6/74 beda, memicu 2 false rule 2160238/Primarizki_kim di univ+luar/lomba+org) — artefak, bukan regression code. | **PASS** — klaim 100% precision tervalidasi di fold, tapi coverage & rule LOW-N = batas klaim robustness | Audit data baru (cert non-UNAIR/FTMM) sebelum klaim robustness; rule LOW-N jangan dipromosikan tanpa validasi |
| **OOD-001** | F0 roadmap v10: OOD probe — (1) template mutation (institusi UNAIR→UNS, FTMM→FST, event hardcode Airnology/Kakiwima/SPECTA/Brief), (2) OCR noise injection confusion nyata (5↔S, 8↔B, 0↔O, 1↔I + merge kata) 0/10/25/50% | Baseline offline (74 cert, no LLM): MACRO exact 55.7% (produksi 47.3% = subset scan 49). **Mutation**: MACRO penuh 55.7→46.9 (−8.9pt) tapi MACRO field bebas-institusi (kegiatan+ tanggal+tingkat) hanya **−1.2pt** (tingkat 81.1→77.0 −4.1pt = sinyal kerapuhan riil); drop organizer 37.8→21.6 & nomor 59.6→23.1 = **terkontaminasi GT** (token institusi dalam jawaban). **Noise**: 10% −8.6pt, 25% −16.4pt, 50% −24.2pt → batas praktis ≈10-25% karakter rusak. | **PASS** (report-only) — v9 lebih robust dr dugaan pada mutation, tingkat & noise = 2 area kerapuhan riil | Rule router yang bergantung pola korpus (`univ+luar`, `hima+luar`) = kandidat revisi Fase 2; OCR noise >10% = butuh normalisasi/fallback |
| **ORG-002** | F1 roadmap v10: normalisasi organizer & nomor pipeline-side (0 LLM). Organizer: strip prefix junk "which held from...by", strip trailing HIMA/BEM bila ada konteks univ, trailing "Universitas"→"Universitas Airlangga". Nomor: pattern ke-4 prefix SERT- + pattern titik + fallback raw text (normalize_text memecah UN27/NACOESTA4.0 → regex gagal) | MACRO exact 55.7→**58.3%** (+2.6), fuzzy 65.1→67.5. **Nomor 59.6→76.9% (+17.3pt, 9 recover)** = GATE PASS (no-regress, 0 LLM). **Organizer 37.8→39.2% (+1.4pt) = GATE FAIL** (target ≥+5pt). Klasifikasi 46 non-exact: 28 fuzzy (PL benar tapi tidak lengkap, mis. Himasada vs GT "Himasada, Fakultas Ilmu Komputer" — masalah ekstraksi, bukan format) + 18 genuine-wrong. Normalisasi matcher v2.1 TIDAK menutup gap (fuzzy sudah contains/overlap, butuh nilai lengkap utk exact). | **PARTIAL PASS** — nomor PASS besar (kandidat kuat), organizer FAIL (normalisasi murni habis) | Organizer sisa gap butuh perbaikan scoring organizer_v2 ATAU fallback LLM per-field (eksperimen A) ATAU KB (Fase 3). Jangan kejar via matcher. Nomor pattern ke-4 + fallback raw text = siap dipakai (produksi ditunda) |
| **ROUTER-004** | F2 roadmap v10: rule mining dari 29 cert unrouted v9 (LLM fallback) + validasi 5-fold. Kandidat: `ukm_org` (organizer UKM tanpa lomba/luar/nasw/sem/fak/dept → Universitas), `hima_dept` (hima_org + dept → Departemen) | **Router coverage 45→50/74 @100% precision** (0 salah, 0 extra LLM call; LLM calls 29→24). `ukm_org`: 3/3 @100% semua fold (LOW-N fire 3). `hima_dept`: 2/2 @100% (LOW-N fire 2; cert SDC Unisba/ACTION yang hima+dept tapi GT Nasional sudah di-route rule hima+luar duluan — guard ordering aman). LLM benar cuma 17/29 (58.6%) di unrouted — LLM tidak superior pada subset ini, rule mining masih bisa menang. Pola tidak konsisten (skip): `luar` saja, `dept` saja, `lomba` saja. | **PASS** — coverage 50/74 @100%, semua gate met (precision ≥95%, 0 extra call, tingkat no-regress) | LOW-N kedua rule (3+2) — validasi data baru sebelum promosi; rule baru bisa diport ke tingkat_router kapan pun (produksi ditunda) |
| **REVIEW-001** | F6 roadmap v10: prototipe extend needs_review ke field lemah (nama_kegiatan 25.7%, nomor 59.6%). Masalah: confidence regex di-set tinggi begitu value ada (0.86/0.95) → field lemah tak pernah ter-flag walau salah. Solusi: confidence berbasis pola (PKKMB 0.95 / struktural 0.60 / keyword-hardcode 0.50 / None 0.0) + ambang per-field (nama_kegiatan 0.55, nomor 0.80) | Cert-level: **recall 98.6%** (68/69 wrong-cert ter-flag), FP = 1 cert (satu-satunya cert exact-all di corpus). Per-field: nama_kegiatan recall 96.7% FP 80% (4/5 — hanya 5 exact total), nomor recall 61.9% FP 0%. **73/74 cert ter-flag butuh review** — trade-off wajib: safety net vs biaya review; roadmap F6 eksplisit "lebih penting dr silent wrong". Keyword hardcode (SPECTA 0/2 exact di corpus) = confidence terendah + kandidat KB. | **PASS** — recall gate ≥60% met; FP absolut 1 cert (refleksi kualitas pipeline v9, bukan bug prototipe) | Sisa salah nomor (23%) tak terdeteksi tanpa GT — butuh sinyal format tambahan ATAU LLM. Prototipe = pola implementasi produksi (produksi ditunda) |
| **F1B-001** | F1b roadmap v10: taksonomi mismatch organizer (45 non-exact: 28 fuzzy + 17 wrong dari pipeline F1) → ceiling riil + daftar perbaikan. Kategori: kurang_lengkap / kelebihan / format_residu / format / salah_org / kosong (heuristik substring + rasio panjang) | Organizer exact 39.2% (29/74). 45 sisa: **kelebihan 10** (ev>GT, residu ikut capture) + **kurang_lengkap 9** (ev benar, GT tambah detail, mis. Himasada) + format 12 (alias UB↔Brawijaya, case, OCR) + salah_org 8 + kosong 4 + format_residu 2. Ceiling per kategori: kelebihan → 52.7%, kurang_lengkap → 51.4%. Temuan kunci: (1) 5/6 kasus `format` = varian case/OCR, bukan matcher; (2) **rule B normalisasi benchmark_org_norm BUG** — `_TRAILING_ORG.sub("", v)` menghapus seluruh string (harus `r"\1"`), jadi normalisasi B/C tidak pernah efektif; (3) `_PREFIX_JUNK` regex `\s+was?` butuh 2 whitespace-run → "Which Held From" tak pernah match. | **PASS** (report-only) — taksonomi = peta perbaikan F1 lanjutan; 2 temuan bug di lapisan F1 | Fokus perbaikan = kategori strip/junk (kelebihan + format_residu + prefix/suffix), BUKAN matcher — dipakai di ORG-003 |
| **ORG-003** | F1 lanjutan roadmap v10: perbaikan pemilihan penyelenggara tanpa AI (lapisan post-processing baru di tests, produksi tak disentuh): R0 fix bug rule B (`r"\1"`), R1 strip suffix ", Faculty/Department" (Inggris SAJA — kasus Indonesia ", Fakultas..." bagian sah GT, 2954707), R2 strip prefix "Library Class", R3 strip prefix sampai "oleh", R4 strip suffix mulai tanggal, R5 normalisasi dash "S-1"→"S1", plus fix regex `_PREFIX_HELD` ("Which [was] Held From <tgl> to <tgl> by") | Organizer exact **39.2% → 45.9% (+6.7pt)** — GATE PASS (≥44.2%). 5 fix per-cert: PRIMARIZKI (rule B), Rasio_Faiz (R1), 2955331 (R4 tanggal), Venedict ORM (R2), Venedict_specta (prefix held). 0 regress organizer. MACRO exact 58.3% → 59.6% (no-regress, naik +1.3). Per-field lain tak ada regress. 0 LLM call. pytest 31 passed. | **PASS** — semua gate met (organizer +6.7pt, MACRO no-regress, 0 LLM) | Lapisan normalisasi v3 = kandidat produksi (keputusan user). Sisa gap: kurang_lengkap (enrich fakultas/prodi — butuh konteks teks), format (alias/OCR — ranah matcher v2.1, keputusan user ditunda), salah_org/kosong (scoring organizer_v2) |
| **F3-001** | F3 roadmap v10: replay KB warm-up prototipe in-memory (opsi B, key = (organizer v3, role) proxy event_type): populate N cert pertama (urutan seeded), eval sisa; tingkat pipeline = extracted_fields v9 (LLM asli tak dijalankan ulang), router = CURRENT recompute (protokol #3), konfig 1x vs 3x confirm | Mekanisme bekerja: LLM calls 31→29 (−2, hit 2 key berulang), tingkat exact eval 84.4/81.5/81.1% (N=10/20/37, ≈ pipeline, selisih = variasi subset), collision 0, size KB 61. **Temuan kunci: korpus 74 hanya punya 2 key (organizer+role) berulang** → gain KB nyata butuh skala besar (36k); asumsi unique organizer 1.000–10.000 (roadmap) BELUM tervalidasi — wajib sampling data riil lintas fakultas sebelum keputusan arsitektur final. Konfig 1x vs 3x confirm identik di korpus ini (tak ada key yang ter-confirm >1x). | **PASS** (prototipe/desain) — mekanisme tervalidasi, dampak terukur butuh data riil; `docs/kb_design.md` = desain final (PG + index PK komposit + mitigasi + skema) | Sampling data riil unique organizer lintas fakultas → kalau asumsi benar, KB bounded (LLM call ~ unique organizer baru per periode). `tests/kb/` → produksi hanya saat promosi (keputusan user) |
| **KB-001** | Shadow replay KB tingkat v1 (alur ROUTER→KB→LLM, key v1: organizer v3+R6 + role map_jabatan + KEY_VERSION, exact-match; conflict → non-authoritative; role kosong → skip). Warm-up N pertama urutan deterministik (sorted-stem), eval sisa; pipeline label = run v9 + router CURRENT; 0 LLM runtime; noise OCR 10/25/50% utk safety | **3x confirm**: key unik 55, key berulang 3 (9 cert) vs F3 2 (raw_role); hit 3 (semua exact vs GT), saved_llm 0; disagree/wrong 0 semua konfig; konflik 0; noise wrong 0. **1x confirm (KOREKSI konfig, lihat KB-002)**: hit 51, saved_llm 13, tapi **wrong 5** — error LLM label terkunci → 1x TIDAK aman. | **PASS (3x)** — no-regress, 0 wrong, konflik tak terpakai; **gain LLM call BELUM terbukti** (3x saved 0 di 74 cert); **1x = GATE FAIL precision** (wrong 5) | Sampling data riil lintas fakultas = prasyarat gate produksi (asumsi 1.000–10.000 unik belum tervalidasi). **Koreksi protokol**: mutasi `WARMUP_CONFIRMS` via package namespace (`from tests import kb`) TIDAK efektif — harus mutasi module (`import tests.kb.kb as kbimpl`); angka F3-001/KB-001 label "1x" sebenarnya 3x default (kesimpulan keduanya tetap valid). Key v1: port produksi harus set KEEP OOD-003 → normalizer beda → KEY_VERSION naik |
| **KB-002** | Seeded persistent KB: (1) smoke seed human_review (authoritative langsung, konflik non-auth, noise→miss, round-trip JSON), (2) ceiling: seed = GT cert pertama utk key berulang (freq≥2), eval semua cert, (3) safety noise 25%, (4) learning persist (snapshot JSON setelah warm → load → eval; harus identik in-memory), (5) fragmentasi key (org logis terpecah varian case/stem) | **Ceiling seeded**: seed 3 keys / 9 certs, hit 9, **saved_llm 0** (key berulang semuanya router-routed), wrong 0, disagree 0; noise 25% wrong 0. **Persistence**: 1x (hits 51=51, wrong 5, saved 13) & 3x (hits 3=3, wrong 0, saved 0) — mem==persist identik. **Fragmentasi**: 1 org logis (FST DEPT) terpecah 2 key (7 cert) → exact-match kehilangan hit. **KOREKSI KB-001/F3**: konfig 1x sebelumnya tidak efektif (mutasi package vs module); re-run 1x = wrong 5 → warm-up ≥3 wajib. | **PASS** — G1 persistence identik, G2 seeded wrong 0 (+noise 0), G3 persist==mem, G4 saved_llm jujur (0 seeded/3x; 13 @1x TAPI wrong 5 → 1x dilarang) | 1x confirm JANGAN dipakai produksi (menyebarkan error LLM label). Kandidat: normalisasi key case/punct/stem ATAU alias table utk fragmentasi; sampling data riil tetap gate utama. `report_data.json` tidak disentuh (preseden v23/v24, butuh keputusan user) |
| **KB-003** | Normalisasi key v2 (fragmentasi): 4 varian diukur terpisah — plain (baseline) / stem (case/punct collapse + singular-stem kata >3 huruf) / alias (tabel eksplisit FST pair, data-driven) / both. Key version `v1-{variant}` (snapshot tak tercampur). Metrik per varian: unique/repeated keys, ceiling seeded (GT cert pertama), collision (key→tingkat pipeline beda, gate 0), shadow 3x N=20, noise 25% | **plain**: 55 key, 3 repeat (9 cert), shadow hit 3. **stem/alias/both**: 54 key, 2 repeat (9 cert, FST pair menyatu), ceiling hit 9 =, **shadow hit 5 (+2 vs plain)**, collision 0, wrong 0, disagree 0, noise wrong 0 — semua varian sama-sama aman di korpus ini | **PASS** — G1 collision 0, G2 seeded+noise wrong 0, G3 hit ≥9 (imbang), G4 shadow no-regress; **normalisasi key menaikkan hit 3→5 tanpa efek samping** | Pilih varian final di data riil: alias = konservatif (1 baris, data-driven), stem = general (over-merge risk di data baru — collision harus di-audit lagi via `tests/kb/audit.py`). Alias saat ini hanya FST pair — tambah saat data baru muncul |
| **KB-004** | Instrumen audit korpus utk gate data riil (`tests/kb/audit.py`): input dir *.txt (format extracted_texts), TANPA GT/LLM/OCR — tingkat pipeline = router → fallback offline map_fields_to_form. Output: unique org/key, repeated keys, collision (key→tingkat beda), fragmentasi org logis, proyeksi saved LLM (3x confirm, warm N pertama). Dry-run selfcheck: korpus 74 harus konsisten KB-001 (55 key, 3 repeated) | Dry-run korpus 74: keys 55==55, repeated 3==3 → **SELFCHECK PASS**. collision 0, saved_llm proyeksi 0 (konsisten KB-001/002 — korpus pembatas). Alat siap dipakai pada korpus lintas fakultas | **PASS** — alat berfungsi, angka konsisten dgn eksperimen KB sebelumnya; `--variant stem/alias/both` tersedia utk audit varian normalisasi | Saat data riil datang: `uv run python -m tests.kb.audit --dir <dir>` → gate produksi (repeat key & collision & saved_llm) langsung terukur tanpa nunggu GT |
| **KB-005** | Event_type utk key (pertanyaan desain #1): 4 varian normalisasi (plain/stem/alias/both) × 3 event (role/jenis_kegiatan/kelompok_kegiatan dari `map_kelompok_dan_jenis`, 0 LLM) = 12 kombinasi; cache label per cert sekali, key dirakit via `make_key`. Metrik: keys/repeat/empty rate, collision (gate 0), ceiling seeded, shadow 3x N=20, noise 25% | **role**: satu-satunya event collision 0 — empty 13, repeat 3(9), ceiling hit 9, shadow hit 3 (plain) / 5 (stem/alias/both), wrong 0, noise 0. **jenis**: empty 52 (70% key mati!), collision 1, wrong 1 → TIDAK layak. **kelompok**: repeat 5 (14 cert, paling tinggi) TAPI collision 1, wrong 2 (5 kategori over-merge) → GATE FAIL. | **PASS (role)** — G1 collision 0 hanya role; G2 wrong 0; G3 shadow hit ≥5 (stem/alias/both); G4 empty rate jujur. **Jawaban desain #1: TIDAK perlu ekstrak `jenis_kegiatan` beneran — role proxy cukup & paling aman** | Keputusan schema produksi KB: pakai role (tanpa kolom event_type baru). Kalau data riil butuh diskriminasi lebih, kelompok_kegiatan hanya layak bila collision-nya di-audit per key (audit.py) |
| **KB-006** | Alias mining data-driven (`tests/kb/alias.py`): kandidat alias = stem-org sama + role sama + tingkat pipeline sama, >1 org label; valid = GT konsisten semua cert group (over-merge detection). Benchmark: precision kandidat vs GT + dampak shadow 3x (plain vs auto-alias vs auto+manual) + noise 25% | Mining menemukan PERSIS kandidat yg sama dgn alias manual KB-003: FST pair `Information System Dept.`→`INFORMATION SYSTEMS DEPT` (2/5), valid=True (GT konsisten). auto-alias: keys 54, seeds 2, ceiling hit 9, **shadow hit 5 (= manual)**, wrong 0, disagree 0; noise 25% wrong 0. | **PASS** — precision kandidat 100% (0 over-merge), shadow hit ≥5, wrong 0; **auto-alias = alias manual di korpus ini (terotomasi tanpa kehilangan keamanan)** | Mekanisme siap utk data riil: `mine_alias_candidates` → review manusia → `make_key(aliases=...)`. Validitas kandidat di data baru wajib diukur ulang (GT korpus baru atau sampling manual) |
| **KB-SCALE-001** | Simulasi workload produksi skewed (efisiensi saat scale, 0 LLM, `tests/benchmark_kb_scale.py`, produksi tak disentuh): 500-5000 request sintetis, sampling key space 55 korpus dgn bobot freq^alpha (alpha dicari agar top-20% key menampung 70/80/90% request), replay ROUTER→KB→LLM (3x confirm, hit hanya authoritative), 2 volume (500 & 5000 req). Pipeline label = `audit.pipeline_label` zero-LLM | **GATE PASS** (80/20, N=5000): saved_llm **87%** non-routed (391/448) ≥50%, hit 97% ≥60%, wrong 2.8% hit <5%. N=500 (sensitivitas): saved 12% → **FAIL volume kecil** — warmup 3x menelan porsi besar; hemat LLM proporsional request per key. Est. latency saved ~978s/5000 req (asumsi 2.5s/call v4 — ESTIMASI). 55/55 key authoritative. Wrong = warisan error pipeline (key populer = benar → error serve 2.8% < pipeline ~19%) | **PASS** — efisiensi KB muncul di volume besar + skew (produksi 36k req / 1-10k key = di tengah); synthetic = proyeksi pola, bukan bukti riil | Gate data riil tetap `tests.kb.audit` lintas fakultas. Report: `docs/report/kb_scale.md` |
| **KB-SCALE-002** | Race tulis multi-worker (`tests/test_kb_scale_concurrency.py` + lock di `tests/kb/kb.py`): 8 thread × 500 write key sama paralel — tanpa lock `confirms` lost-update (entry tak pernah authoritative walau 3x / conflict palsu). Fix: `threading.Lock` per TingkatKB di write/lookup/peek (produksi tetap zero-touch) | 2 test: key sama (confirms == 8×500, authoritative) + 20 key acak-deterministik (per key == 8×300). Sebelum fix: test gagal (lost update) — setelah lock: deterministik, 0 lost update | **PASS** — write aman utk multi-worker (api + db_worker); PG production tetap jadi otoritas (lock = prototipe in-memory) | Lock global cukup di skala ini; per-key lock bila profiling butuh (ponytail) |
| **KB-SCALE-003** | Peta hemat KB (volume × key space × skew) (`tests/benchmark_kb_scale_map.py`, 0 LLM, produksi tak disentuh): grid 5 volume {500..36k} × 5 key space {55..10k} × 3 skew, key sintetis freq zipf, routed share 61% (terukur korpus), confirm 3x. Memvalidasi asumsi kb_design (1k-10k unique organizer utk 36k request) | **GATE PASS**: 36k request @80/20 → saved **92% (1k key) / 78% (5k) / 72% (10k)**, semua ≥50%. Peta penuh: volume kecil + key space besar = hemat rendah (500 req / 10k = 35%). Insight: hemat dari key POPULER (top-20% menampung 80% request) — request/key rata-rata global menyesatkan; 36k/10k = 3.6 req/key rata-rata TAPI tetap 72% | **PASS** — asumsi kb_design layak (dgn skew); cold-start worst case | KB persist lintas periode mengubah matematika (warmup sekali, hemat selamanya). Report: `docs/report/kb_scale_map.md` |
| **KB-SCALE-004** | Konfigurasi KB: confirm 2x vs 3x + key alias (`tests/benchmark_kb_scale_conf.py`, 0 LLM, korpus 74): grid confirm {2,3} × key {plain, alias} × N {500, 5000} × skew 80/20. Gap: 2x belum pernah diuji (1x sudah gagal wrong 5, KB-002) | **GATE FAIL**: wrong 2x **304** vs 3x **284** (naik +20 — error pipeline terkunci lebih cepat); hemat N=500 naik (24% vs 5%) TAPI tidak sebanding dgn salah ekstra. Alias: keys 55→54, hit naik tipis di N=500 (409 vs 407 @2x), efek kecil (pair non-populer) | **FAIL** — 3x confirm TETAP wajib; 2x hemat palsu (salah ikut naik) | 2x jangan dipakai produksi (konsisten KB-002: confirm lebih sedikit = error tersebar). Report: `docs/report/kb_scale_conf.md` |
| **KB-SCALE-005** | Proyeksi akurasi & biaya produksi end-to-end (`tests/benchmark_kb_scale_e2e.py`, 0 LLM): model dgn nilai TERUKUR — router 100% (45/45), KB serve 97.2% (SCALE-001), LLM 58.6% (17/29 non-routed), 176 tok/call (v9); grid N {500,5000} × skew {70-90%} | **GATE PASS** (80/20, N=5000): akurasi **96.3% → 99.3%** (dgn KB ≥ tanpa KB, semua kombinasi), hemat biaya **87%** (57 vs 446 calls). Insight kunci: **KB = cache yang LEBIH akurat dari LLM** (serve 97.2% vs fallback 58.6%) — hemat + akurasi naik sekaligus | **PASS** — KB menaikkan akurasi akhir & hemat biaya ≥50% di produksi | Proyeksi label korpus + distribusi simulasi; validasi akhir audit.py data riil. Report: `docs/report/kb_scale_e2e.md` |
| **KB-PROD-001** | Semantik produksi utk gap yang eksperimen tak bisa jawab (`tests/kb/kb.py`, 0 LLM, produksi zero-touch): (1) `servable()` source-aware — `AUTHORITATIVE_CONFIRMS = {router_rule: 3, llm: 5, human_review: 1}` (llm label butuh bukti lebih sebelum autofill; `authoritative` lama TIDAK diubah — angka KB-001..006/SCALE tetap valid) + TTL `last_verified_at` (default 365 hari, entry basi = non-servable); (2) `resolve()` — jalur human review utk entry konflik (sebelumnya mati selamanya); (3) `audit_due()` — sampling protokol F0 (prioritas source=llm + conflicts>0) | `tests/test_kb_production_semantics.py` 4 test + demo: llm 3x = authoritative legacy TAPI non-servable → 5x servable; human_review resolve langsung servable; TTL 365 non-servable utk entry 2020, TTL None = tanpa batas; resolve membuka entry konflik; audit_due = llm+konflik saja (router_rule bersih tak masuk). Pytest **60 → 64 passed** | **PASS** — semantik produksi ter-kode di prototipe, 0 regress eksperimen lama | Gap tersisa utk port: (a) PG atomic upsert ganti threading.Lock (multi-proses), (b) hit_count counter batch (write contention), (c) ukur ulang tok/call path produksi (f_bias, bukan 176 v9), (d) gate data riil audit.py. kb_design.md §4/§6 diperbarui |
| **AKT-002** | Deteksi nama_kegiatan v2 (eksperimen ber-gate, 0 LLM, produksi tak disentuh, `tests/benchmark_akt2.py`): extractor `extract_activity_v2` = 16 anchor pattern baru + repair OCR word-merge + strip junk, di atas `offline_prod` (PROD-002). Berbasis taksonomi AKT-001 (kosong = deteksi) | **GATE PASS** (GT v9 + matcher v2): nama_kegiatan exact **6.8→36.5% (5→27/74, +29.7pt, 22 fix)**, fuzzy 17.6→56.8%, MACRO **63.0→68.8%**, 0 regress, 0 LLM. Anchor: Kepengurusan full-phrase (VENEDICT_sertif_bem), Dalam acara/kegiatan/memperingati, pada ajang/kegiatan/kompetisi, `\bpada X yang dilaksanakan` (guard \b fix false-positive "KEPADA"+nama), entitled/titled (Ananda×5), kutip agenda/kegiatan (SERTIF76, UNITY, 1963507 — konflik konvensi "Mawacana" teratasi: GT pilih judul kutip), participation at, the X organized by (Rasio/Poisson), winner of the, as a participant at, at the X, strip junk "untuk kategori". Repair merge: dalam/of/yang/oleh/kompetisi/masa/bakti/pada + batas digit-huruf (Science2025). **OOD noise (QA): drop ekstra 10% +2.7pt, 25% +8.1pt** (anchor teks-bergantung, pola R1/R4 OOD-002/003); gain absolut positif semua level (10%: 32.4% vs 5.4%). BUG ditemukan saat iterasi: `re.IGNORECASE` global memecah camel-split (?<=[a-z])(?=[A-Z]) → spasi antar semua huruf (harus per-pattern). | **PASS** — semua gate met (+29.7pt ≥ +5pt, no-regress, 0 LLM) | Port produksi = keputusan user; anchor = GUARD needs_review F6 (noise-rapuh); matcher quirk: nilai ber-junk bisa dapat exact credit via abbreviation (rasio ≤0.6) sedangkan nilai bersih tidak (2954631) — jangan kejar metrik via junk. Report: `docs/report/akt2_activity_detection.md`. |
| **AKT-003** | Deteksi nama_kegiatan v3: grup A+B+C (eksperimen ber-gate, 0 LLM, `tests/benchmark_akt3.py`, produksi tak disentuh). A: `Sebagai Peserta X` (7 fix: Public Health ×2, Airlangga Job Prep, Magang UKM ×3, KIM); B: `Sebagai Peserta "X"` kutip (ORM exact, ACIC fuzzy); C: repair `atthe`/`inthe`/`AT THE`/`organizedby`/digit-merge (Hology exact, Poisson exact, FIT partial) + stop `with theme` di anchor at-the | **GATE PASS** (GT v9 + matcher v2): nama_kegiatan exact **6.8→51.4% (5→38/74, +13.5pt dari AKT-002, +44.6pt kumulatif)**, fuzzy 17.6→74.3%, MACRO **63.0→71.6%**, 33 fix / 0 regress, 0 LLM. Robustness: prefix `Sebagai` WAJIB (anti header/role "Peserta"), stop bare `Dalam|Pada` + negative lookahead `Talkshow|campaign` (anti junk-capture, 2398703/Venedict → None), newline-boundary stop (KIM capture utuh melewati \n), `\s+Universitas(?=[A-Z])` (ppkmb merged-junk), `\s+yang\s+(diselenggarakan|...)` + repair `yang`/`oleh` digit-merge (ACW/hakim bersih). **OOD noise (QA): drop ekstra 10% +4.1pt, 25% +9.5pt** (lebih tinggi dari AKT-002 — anchor Sebagai Peserta teks-bergantung; gain absolut positif semua level: 10% 45.9% vs 5.4%). Changed non-exact 5 (semua wajar: ACIC fuzzy, 2954571 "Lomba Infografis" plausible-partial in_text=tidak, ACW "2026" vs GT "2025" = GT v10, hakim OCR "GRADl ANT", FIT all-caps tak bisa di-split aman). | **PASS** — gate met (+13.5pt ≥ +5pt, no-regress, 0 LLM) | AKT-004 (grup D+E+F+G: all-caps DALAMACARA, dalam Event/lomba, seminar on, junk yang) + KARSA/ACW/hakim = GT v10/OCR. Port produksi = keputusan user (pola KEEP/GUARD — noise-rapuh). Report: `docs/report/akt3_activity_detection.md`. |
| **AKT-004** | Deteksi nama_kegiatan v4: grup E+F+G (eksperimen ber-gate, 0 LLM, `tests/benchmark_akt4.py`, produksi tak disentuh). E: `dalam Event X` (Girifest — capture ikut "Event", stop " dengan"); E2: `lomba tingkat Nasional "X"` (SDC Uniska); F: `seminar on "X"` (2954283); G: verifikasi junk yangdiselenggarakan (sudah bersih di AKT-003). Repair OCR-alias `Nis C`→NISC (camel-split pecah "NisC" — pasca-repair) | **GATE PASS (gate scope-kecil >=+2 exact, keputusan user — handoff v33)** (GT v9 + matcher v2): nama_kegiatan exact **51.4→54.1% (38→40/74, +2 exact)**, MACRO **71.6→72.1%**, 35 fix / 0 regress, 0 LLM. Girifest exact + 2954283 exact. SDC Uniska = WRONG (cert tulis "Statistic", GT "Statistics" — beda teks vs GT → **kandidat GT v10**, bukan bug kode). OOD noise: drop ekstra 10% +4.1pt / 25% +9.5pt (sama pola AKT-003, anchor E/F fire sedikit → tidak menambah drop). Changed non-exact 1 (SDC Uniska, wajar). | **PASS** — gate met (+2 exact, no-regress, 0 LLM) | AKT-005 (grup D: all-caps DALAMACARA + huruf berjarak — synreaach, Gelar Rasa, 2954685, BINARY, SDC Unisba, ACIC; risiko tertinggi). Report: `docs/report/akt4_activity_detection.md`. |
| **AKT-005** | Deteksi nama_kegiatan v5: grup D all-caps/spacing (eksperimen ber-gate, 0 LLM, `tests/benchmark_akt5.py`, produksi tak disentuh). Repairs pre-camel: collapse run huruf-berjarak >=3 (SDC Unisba), split keyword all-caps (DALAM/ACARA/DEKAN/CUP/BY/SYNREACH/GELAR — synreaach, Gelar Rasa; SPEAK/STAND/MENGASAH/KOMUNIKASI/DI/ERA/KARIR — ACIC), UXDESIGN + `(?i)competition` + `(?i)dengan` (2954685); post-camel: re-split competition (anti bunuh camel-boundary), sentinel `\n` sebelum "Himpunan" digit-bound; anchor join2 (kutip+sisa `[...]`, span newline) DI ATAS anchor kutip lama | **GATE PASS** (GT v9 + matcher v2, no-regress juga vs AKT-004): nama_kegiatan exact **54.1→62.2% (40→46/74, +6 exact)**, MACRO **72.1→73.7%**, 41 fix / 0 regress, 0 LLM. Fix: synreaach, Gelar Rasa, 2954685, SDC Unisba, ACIC, +1. **OOD noise: drop ekstra 10% +4.1pt, 25% +13.5pt** (NAIK vs AKT-004 — grup D paling noise-fragile: collapse & all-caps = sinyal mudah rusak; gain absolut positif semua level). 2 regress vs AKT-004 tertangkap saat iterasi (competition lowercase membunuh camel-boundary 2954631/Poisson → post-repair re-split). BINARY_Venedict SKIP: OCR "Bl NARY 2o 23" vs GT "BINARY 3.0" (angka beda → kandidat GT v10/konvensi, bukan fix regex aman). Changed non-exact 1 (SDC Uniska, wajar). | **PASS** — gate met (+6 exact ≥ +5pt, no-regress, 0 LLM) | Rangkaian AKT SELESAI (001 report-only + 002/003/004/005 PASS, kumulatif 6.8→62.2%). Port produksi = TIDAK — keputusan user (tetap eksperimen; bila port: KEEP/GUARD F6, grup D paling berisiko). Report: `docs/report/akt5_activity_detection.md`. |
| **AKT-001** | Taksonomi mismatch `nama_kegiatan_sertifikasi` (0 LLM, `tests/akt1_activity_taxonomy.py`, mirror F1b): klasifikasi 69 non-exact jalur produksi PROD-002 (offline_prod, flag ON), kategori kosong/salah_kegiatan/kurang_lengkap/kelebihan/format_ocr + kolom `in_text` (apakah GT ada di teks raw) | Exact **5/74 (6.8%)**. Taksonomi: **kosong 57** (52/57 GT MUNCUL di teks → deteksi regex feasible, tinggal pattern coverage; 5 tidak ada di teks → butuh LLM/OCR), salah_kegiatan 4 (PKKMB/Specta/Mawacana false-positive keyword hardcode), kurang_lengkap 3, kelebihan 3 (junk suffix "Untuk Kategori...", "Pada Perlombaan..."), format_ocr 2. Anchor pattern baru terlihat di teks: "Dalam acara X", "pada ajang X dengan tema", "As a participant at X", "Dalam memperingati X yang diselenggarakan". | **PASS (report-only, preceden F1B)** — peta ceiling: kosong = 83.8% ceiling; masalah = DETEKSI, bukan normalisasi (beda fundamental dari organizer) | AKT-002: perluas pola regex activity name (Indonesia+Inggris, anchor "Dalam acara/pada ajang/as a participant at/dengan tema") + repair OCR-merge (pola `_preprocess` organizer_v2) + guard keyword hardcode (SPECTA/BRIEF dll hanya fire bila tak ada pattern lain). Report: `docs/report/akt1_activity_taxonomy.md`. |
| **PROD-002** | Port produksi lapisan eksperimen 0 LLM (keputusan user handoff v29): `backend/app/services/organizer_normalize.py` + flag `ENABLE_ORGANIZER_NORMALIZATION` default FALSE di `extraction_pipeline.py`. KEEP R0/PREFIX_HELD/R2/R3/R6 + F1 alias + F3 BEM FKM + nomor D/E; SKIP R1/R4 (2 fix dikorbankan, 0 risiko); DROP R5. Re-eval GT v9 + matcher v2 (`tests/benchmark_prod_port.py`): **port fidelity prod==ref 0 mismatch (28 cert × 6 field)**, baseline produksi current (offline_fields) → PROD path: organizer **37.8→63.5% (47/74)**, nomor **59.6→76.9% (40/52)**, MACRO **55.7→63.0%**, 28 fix / 0 regress, 0 LLM. Smoke pipeline: flag OFF inert, flag ON R0 strip trailing HIMA (PRIMARIZKI_binary_2025 → GT exact). pytest 58 passed (31 lama + 27 baru `test_organizer.py`). **KOREKSI spec handoff: R3 (strip sampai "oleh") BUKAN dead code** — ablation OOD-002/003 "R3 = NO_FIX" diukur pre-ORG-004 (v3 tanpa lapisan alias); di port final R3 justru membuka alias F1 APHSA (1952296/2030335 = 2 fix). | **PASS** — semua gate met (fidelity, no-regress, smoke flag) | Flip flag produksi = keputusan user setelah re-eval disetujui; R1/R4 bisa dibuka bila mekanisme GUARD (needs_review F6) diport. Report: `docs/report/prod_organizer_port.md`. |
| **OOD-002** | OOD probe lapisan organizer v3 (0 LLM, `tests/ood_probe_v3.py`): SAMA input dgn OOD-001 (mutation + noise 10/25/50%) dievaluasi atas pipeline v9 & v3 → bandingkan drop (gate: drop_v3 ≤ drop_v9 + 1pt mutation/10%; 0pt di 25/50%) + ablation per aturan (disable 1 rule) + noise survival fix-cert per aturan | Baseline: v3 59.6% MACRO / organizer 45.9% (konsisten ORG-003). **Mutation: extra drop v3 = 0 kerapuhan aturan nyata** (drop organizer 20.3% vs v9 16.2% = 100% kontaminasi GT — nilai v3 justru mengikuti institusi baru, mis. '...Universitas Negeri Semarang...', GT lama tak di-update; free_inst & tingkat v3 = v9 PERSIS). **Noise: 2 aturan rapuh teks-bergantung** — R4 (mati sejak 10%: 'July 30'→'July 3O' digit rusak; survival 0%) & R1 (mati sejak 25%: kata merge 'Facultyof' vs regex koma+spasi); R0/R2/PREFIX_HELD bertahan s.d. 50%. Extra drop organizer noise: 10% +1.4pt, 25% +1.3pt, 50% +4.1pt (50% = 2 PRIMARIZKI matcher-digit 'S1'→'SI' BUKAN aturan strip + 1 R4). Ablation: R3 = NO_FIX (ubah 2 nilai tanpa perbaikan), R5 = DEAD (tak pernah ubah nilai), semua aturan HANYA 1 fix-cert (LOW_N <3) → klaim robustness per-aturan butuh data baru. | **FAIL (gate drop-relatif)** — kerapuhan nyata terbatas: 0 di mutation, R4+R1 di noise ≥10-25%; gain absolut v3 tetap positif di semua kondisi (noise 10%: 44.5% vs v9 37.8%) | Port produksi: R0/R2/PREFIX_HELD KEEP, R1/R4 = GUARD (wajib needs_review F6), R3/R5 hapus (tanpa manfaat terukur). Validasi data baru (cert non-UNAIR/FTMM) sebelum klaim robust per-aturan |
| **F1C-001** | F1 lanjutan roadmap v10: enrich `kurang_lengkap` organizer dari konteks teks (0 LLM, lanjutan ORG-003): R6 = (1) join baris beruntun berkata-kunci org di bawah baris yang = v persis (2954933: FakultasMatematika+UGM; camel-split + typo OCR `llmu`→`ilmu`), (2) Biro-prefix (SERTIF76), (3) Himasada + baris `DEKAN FAKUETASILMU KOMPUTER` → `, Fakultas Ilmu Komputer`, (4) IRIS: baris org tepat di atas baris fakultas → prepend, (5) dept OCR-merge `INFORMATIONSYSTEMSSTUDYPROGRAM` → `Information System Dept.` (Ananda Agentic AI + E-certificate (1)). Guard keras: v tanpa penanda fakultas/kampus, baris lanjutan beruntun max 2, dedup ≠ v | Organizer exact **45.9% → 54.1% (+6.2pt dari ORG-003; 39.2% → 54.1% dari baseline F1, 11 fix)** — GATE PASS (target ≥51.0%). 6 fix R6: 2954933 (join+typo), IRIS (prepend), SERTIF76 (Biro), Gelar Rasa (Himasada fakultas), Ananda Agentic AI + E-certificate (1) (dept merge). 0 regress organizer (verifikasi empiris risiko: Piagam HIMA S1-AK, 2065179, UKM×2, 2439919, Venedict_specta — guard baris beruntun/berkata-kunci bekerja). MACRO exact 59.6% → **61.2%** (+1.6, no-regress). 0 LLM. pytest 31 passed. Kasus tak fixable dari teks (skip v1): 2160238 (masalah scoring — extracted cuma institusi), NIC_Faiz (OCR merge parah), SDC Unisba (OCR spasi hancur). | **PASS** — semua gate met (organizer +6.2pt, MACRO no-regress, 0 LLM, 0 regress) | R6 = kandidat produksi bersama lapisan v3 (KEEP R0/R2/PREFIX_HELD/R6, GUARD R1/R4, hapus R3/R5). Re-run OOD probe v3 sebelum port — R6 teks-bergantung, ekspektasi rapuh di noise. Sisa gap: format (matcher v2.1, keputusan user ditunda), salah_org/kosong + 2160238 (scoring organizer_v2) |
| **ORG-004** | F1 lanjutan (jalur akurasi, redirect dari KB): canonicalisasi bucket `format` di atas v3 (`tests/benchmark_org_format.py`, 0 LLM, produksi tak disentuh): F1 alias map compact-value (IS DEPT 5 cert, APHSABEMFKM, STUDISL, Facultyof+UB, Majudan), F3 suffix "BEM FKM"→"...Universitas Airlangga" (konteks UNAIR). F2 (hapus "dan" FMIPA) & F4 (BEMFTMM→BEM FTMM) & HIMASTA-expand **dibatalkan** | Organizer exact **54.1% → 66.2% (+12.1pt, 9 fix)** — GATE PASS (≥+5pt), MACRO **61.2% → 63.5%** (+2.3, no-regress), 0 regress organizer, 0 LLM. OOD: mutation drop v4 −10.9% vs v3 −10.4% (+0.5pt, dalam toleransi); noise 10/25/50% drop IDENTIK v3. Fix: Ananda×5 (IS DEPT), 1952296+2030335 (APHSA BEM FKM), BINARY (STUDISL), 2439919 (Facultyof+UB), ACTION −"dan" **tanpa F2** (fuzzy, bukan exact). **Temuan: GT inkonsisten** — FMIPA "dan" (ACTION tanpa, 2954933 dengan) & HIMA (Himatesda vs Himpunan Mahasiswa Teknologi Sains Data) → rule tersebut merusak (F2 regress 2954933, HIMASTA-expand regress 12) | **PASS** — semua gate met (organizer +12.1pt, MACRO no-regress, OOD drop dalam toleransi) | Lapisan canonical = kandidat produksi (keputusan user). F2/F4/HIMASTA JANGAN dipakai — GT inkonsisten; kandidat GT v10 (bukan pipeline). Sisa: salah_org 8, kosong 4, format 2, kelebihan 5, kurang_lengkap 4 |
| **ORG-005** | Probe scoring organizer_v2 (sisa 12 kasus: salah_org 8 + kosong 4): (1) pattern phrase baru "diselenggarakan pada tanggal X oleh Y" via monkeypatch extractor — fix 0 (greedy pattern gagal, perlu surgery extractor), (2) fallback regex "oleh X" saat kosong — fix 1 (Hitech), no-cand 3 (AQEEL, data slayer, BTF), (3) HIMASTA→full expand — **regress 12** (GT tak konsisten: Himatesda vs Himpunan Mahasiswa...) | Probe: hanya 1/12 fixable murah (Hitech "oleh Politeknik Caltex Riau"); sisa 11 butuh rewrite scoring extractor (blast radius luas — extractor dipakai semua benchmark organizer) dengan risiko regress tinggi (terbukti HIMASTA). Full run TIDAK layak di korpus 74 tanpa GT baru. | **FAIL (probe)** — perbaikan aman hanya 1 kasus; rewrite scoring = risiko > gain | Re-try saat data/GT baru (inkonsistensi GT v9: FMIPA "dan" 2954933, HIMA pendek/panjang) ATAU keputusan user utk rewrite scoring extractor + re-baseline penuh |
| **OOD-002** | OOD probe lapisan organizer v3 (0 LLM, `tests/ood_probe_v3.py`): SAMA input dgn OOD-001 (mutation + noise 10/25/50%) dievaluasi atas pipeline v9 & v3 → bandingkan drop (gate: drop_v3 ≤ drop_v9 + 1pt mutation/10%; 0pt di 25/50%) + ablation per aturan (disable 1 rule) + noise survival fix-cert per aturan | Baseline: v3 59.6% MACRO / organizer 45.9% (konsisten ORG-003). **Mutation: extra drop v3 = 0 kerapuhan aturan nyata** (drop organizer 20.3% vs v9 16.2% = 100% kontaminasi GT — nilai v3 justru mengikuti institusi baru, mis. '...Universitas Negeri Semarang...', GT lama tak di-update; free_inst & tingkat v3 = v9 PERSIS). **Noise: 2 aturan rapuh teks-bergantung** — R4 (mati sejak 10%: 'July 30'→'July 3O' digit rusak; survival 0%) & R1 (mati sejak 25%: kata merge 'Facultyof' vs regex koma+spasi); R0/R2/PREFIX_HELD bertahan s.d. 50%. Extra drop organizer noise: 10% +1.4pt, 25% +1.3pt, 50% +4.1pt (50% = 2 PRIMARIZKI matcher-digit 'S1'→'SI' BUKAN aturan strip + 1 R4). Ablation: R3 = NO_FIX (ubah 2 nilai tanpa perbaikan), R5 = DEAD (tak pernah ubah nilai), semua aturan HANYA 1 fix-cert (LOW_N <3) → klaim robust per-aturan butuh data baru. | **FAIL (gate drop-relatif)** — kerapuhan nyata terbatas: 0 di mutation, R4+R1 di noise ≥10-25%; gain absolut v3 tetap positif di semua kondisi (noise 10%: 44.5% vs v9 37.8%) | Port produksi: R0/R2/PREFIX_HELD KEEP, R1/R4 = GUARD (wajib needs_review F6), R3/R5 hapus (tanpa manfaat terukur). Validasi data baru (cert non-UNAIR/FTMM) sebelum klaim robust per-aturan |
| **OOD-003** | Re-run OOD probe v3 dengan R6 (F1C-001) — RULE_ORDER + "R6" → ablation & noise survival mencakup R6 (sumbu 1+2 sudah otomatis); input & seed identik OOD-002 | Baseline v3: organizer 45.9% → **54.1%**, MACRO 59.6% → **61.2%**. **R6 = KEEP tanpa catatan GUARD**: fix 6, regress 0, survival 100% di noise 10%, **0 fix R6 mati di semua kondisi** (mutation non-kontaminasi + noise 10/25/50%) — lebih robust dari R1/R4 (compact-equality tahan char-confusion, typo map OCR menangani merge, join beruntun tak bergantung spasi). Mutation: drop organizer v3 21.6% vs v9 16.2% = **100% kontaminasi GT** (4 cert: PRIMARIZKI×2, SERTIF76 fix-R6, Venedict ORM — GT memuat token institusi); free_inst/tingkat v3 = v9 PERSIS. Noise: extra drop vs v9 **identik OOD-002** (10% +1.4, 25% +1.3, 50% +4.1pt) — R4 (2955331, sejak 10%), R1 (Rasio_Faiz, sejak 25%), 50% = matcher digit S1→SI (PRIMARIZKI×2, bukan aturan strip). R3 NO_FIX, R5 DEAD, R0-R4 LOW_N (1 fix-cert) | **FAIL (gate drop-relatif)** — struktur kerapuhan SAMA dgn OOD-002; **R6 menambah 0 kerapuhan**; gain absolut v3 tetap positif di semua kondisi (noise 50%: 41.9% vs v9 29.7%) | Port produksi final: KEEP R0/R2/PREFIX_HELD/**R6**, GUARD R1/R4 (needs_review F6), hapus R3/R5. R6 (6 fix ≥3) = satu-satunya aturan dengan cukup fix-cert untuk klaim robust di level korpus; validasi data baru tetap wajib untuk generalisasi |
| **HYB-KB-001** | Efek KB pada pipeline HYB-LLM-001 (korpus 74, replay online ROUTER→KB→LLM, label dari artefak run 20260821_135619, 0 LLM runtime, produksi zero-touch): hit hanya utk cert unrouted + entry `servable()` (KB-PROD-001: router_rule 3x / llm 5x), warm-up alami tanpa split | Exact **60/74 = baseline persis** (recompute matcher current; summary runtime resmi 62/74 & LLM 17/26 — artefak per-cert = bound konservatif 15/26), wrong 0, saved_llm 0 (DIHARAPKAN: korpus cuma 3 key berulang ≤3 kemunculan; servable butuh ≥4 kemunculan utk 1 hemat; llm-source butuh 5 confirm), kb_size 55, 13 cert tanpa key (org/role kosong) | **PASS** — G1 no-regress exact, G2 wrong=0; efek nyata KB bukan di korpus pembatas ini | Gain di korpus mungkin bila data riil punya key berulang lebih banyak (audit.py) |
| **HYB-KB-002** | Proyeksi skala ril user (~360.000 request NON-concurrent, horizon 3 thn; `tests/benchmark_hyb_kb_scale.py`, 0 LLM): grid key space {1k,5k,10k,50k} × skew {70/30–90/10} × TTL {∞, 365h}, profil key di-clone dr korpus (inherited error riil), hit=servable(), TTL dimodelkan harness-side (kb.py tak disentuh) | **GATE PASS** @(keys 10k, 80/20, tanpa TTL): hemat **86,4%** LLM calls (101.446/117.349), akurasi **86,2% → 89,5% (+3,3pp)**, ≈42 jam proses & ~17,9 jt token terhemat; hemat grid 72–98% semua kombinasi ≥50%; pelajaran iterasi: versi awal melayani KB utk request ROUTED → akurasi jeblok −10pp (error turunan KB menimpa router @100%) — fix: router SELALU menang, KB hanya unrouted | **PASS** — hemat ≥50% DAN akurasi naik di SEMUA titik grid | Angka final butuh sampling data riil via `tests/kb/audit.py` (frontier handoff v36) sebelum port produksi |


---

## Aturan Pengisian

1. **Satu baris per pendekatan tertutup** (engine, prompt variant, strategi).
2. Kolom `Hasil` = angka terukur (jangan opini).
3. Kolom `Re-try condition` = kondisi eksplisit yang membuat pendekatan layak
   dicoba ulang. Jika tidak ada, tulis "tidak ada".
4. Commit bersama kode eksperimen & update `runs_summary.md` (workflow B9-B11).
5. Referensi detail: `docs/handoff_v28.md` + `docs/report/runs_summary.md`.

| **HYB-COMBINED-001** | Gabungan terbaik: router tingkat (48/74 routed) + AKT-005 nama_kegiatan (62.2%) + ORG-004 organizer (63.5%) + PROD-002 nomor (76.9%) — 0 LLM, offline | MACRO exact **63.0→73.7% (+10.7pp)**, fuzzy 68.0→80.2%, nama_kegiatan +55.4pt, 0 regress per-field, router 48/74 @100% | **PASS** — semua gate met, new winner vs v9 (60.2%). 0 LLM call, 0 tokens | Full run 74 cert; hybrid gabungan semua improvements terbaik; tingkat dari router rules saja (81.1%, tidak pakai LLM); 26 cert need LLM untuk tingkat |
| **HYB-LLM-001** | HYB-COMBINED + LLM fallback untuk 26 cert unrouted (llama3.1:8b) | MACRO exact **73.7→74.2% (+0.5pp)**, fuzzy 80.2→80.7%, tingkat **81.1→83.8% (+2.7pt)**, 0 regress, LLM 17/26 correct (65.4%), 1.5s/cert, 26 calls ~176 tok/cert | **PASS** — semua gate met, new overall winner. Tingkat naik ke level v9 (83.8%) tanpa regress field lain. 26 LLM calls (hemat 65% vs v9 29 calls) |
| **OCR-008** | Revive LFM2.5-VL-3B full-scan (lanjutan re-try OCR-007): transkripsi SEMUA 49 stem scan via `--all-scans` (`tests/benchmark_lfm25_probe.py`), GGUF Q4_0 + mmproj Q8_0, llama-server b10405, temp 0.1; eval otoritatif scan-49 + korpus komposit all-74 (LFM 49 scan + baseline 25 embedded — embedded tak pernah lewat OCR di produksi) | 49/49 ok, 0 error, ~77–95s/cert, server RSS ~3.9GB. Scan-49 like-for-like GT v9 + matcher v2: **nomor 51.5%** (17/33, baseline 57.6% → regress −6.1pt), dates **85.7% (=)**, **organizer exact 40.8%** (20/49, baseline 26.5% → **+14.3pt**, fuzzy 63.3%), MACRO exact **49.75%**. Komposit all-74: MACRO exact **50.65%** vs pembanding resmi 47.3% (**+3.35pt**), fuzzy 60.97%, nomor 57.7% (=). Run: `ocr_experiment/lfm25_full_20260824_111939` + `composite_lfm_full_20260824_111939` | **PASS (measurement)** — angka terekam lengkap; output = input HYB-002. Catatan: `/tmp/opencode/` ter-wipe di tengah run (model + binary hilang) → re-download ke `~/.cache/lfm25/` (persisten); batch resume via skip-if-exists tanpa hitung ulang | Hybrid per-field (HYB-002); produksi butuh F7 OCR queue (~77s/cert, RSS ~4GB) |
| **HYB-002** | Hybrid per-field LFM (pola HYB-001): LFM2.5 utk organizer+dates (DOC_PRIORITY), baseline rapid_tess utk nomor/kegiatan/role; merge level extracted-dict rule statis `merge_fields` TIDAK berubah; eval via `--doc-label lfm25` | 49 scan GT v9 + matcher v2: **semua gate PASS** — nomor **57.6% (=**, merge pertahankan baseline), dates **85.7% (=)**, organizer exact **26.5→40.8% (+14.3pt)**, MACRO exact **47.26→50.75% (+3.49pt)** — mengalahkan hybrid DocTR HYB-001 (46.8%, +3.95pt). 10-stem: repro persis OCR-007 (MACRO 40.0→44.4, org 0→20%). Oracle = hybrid (merge optimal). Embedded fallback baseline no-regress by construction | **PASS** — winner baru cabang OCR (50.75% scan / kompotabel dgn komposit LFM 50.65% tapi nomor utuh) | Produksi butuh keputusan user: LFM batch-only (77s/cert + RSS 4GB); opsi jalur HYB-KB/LLM lanjutan di frontier v38 |
| **NC-003** | Bikin NC-001 viable murah: (a) line-splitting proyeksi horizontal → tesseract psm7 per strip region multi-baris (`split_lines_projection` + `ocr_tess_lines_psm7`); (b) stage timing per tahap; (c) anchor cache JSON `{stem: bbox}` skip full-page RapidOCR saat trial ulang. Engine baru: `tess_lines_psm7`, `rapid_tess_psm6` (kombinasi belum diuji NC-002) | Full 49 scan vs control rapid_tess crop **60.6%**: tess_lines_psm7 **nomor 57.6%** (FAIL, −3pt), avg 4.43s/cert (stages: render 0.37 + **anchor 3.54** + rrender 0.04 + region 0.55); rapid_tess_psm6 **nomor 54.5%** (FAIL, −6.1pt), avg 2.86s (render 0.37 + anchor 0.85* + region 1.64; *17/49 cache-miss saja yg bayar anchor, 32 hits terbukti). Jawaban re-try NC-002: latency floor memang anchor full-page (bukan engine region) — region murah 0.55–1.64s; psm7 multi-baris sudah bisa tapi tetap tak recover digit sebaik multi-config rapid_tess. Anchor cache hidup & identik (bbox sama, teks identik) | **FAIL/CLOSED — tak ada engine murah yg pertahankan +3pt; NC-001 tetap OFF** | Re-try: control multi-config rapid_tess + anchor cache ≈ 0.37+~1.7 ≈ **~2.1s/cert proyeksi** (borderline gate ≤2.0s) — layak 1 trial eksplisit kalau mau flip flag; atau engine baru yg recover digit di strip baris |
| **HYB-003** | Hybrid organizer TANPA LFM: rule `merge_fields` sama (DOC_PRIORITY), tapi doc-engine = teks **rapid-only** — engine yg memang sudah dijalankan baseline sebelum digabung tess; eval offline artefak existing `baseline_rapid/` (`--doc-label rapid`, run `hybrid_rapid_org`) = 0 OCR baru, 0 detik komputasi tambahan | 49 scan GT v9 + matcher v2: organizer exact **26.5→34.7% (+8.2pt)**, fuzzy 61.2→**77.5%** (terbaik semua varian); nomor **57.6% (=)**, dates **85.7% (=)**, nama_kegiatan 6.1% (=); MACRO 47.26→**49.2% (+1.94pt)**. Kalah tipis dr HYB-002 LFM (50.75% — gap +1.55pt persis = selisih organizer LFM vs rapid) tapi **mengalahkan HYB-001 DocTR hybrid (46.8%)** yg butuh +8s/cert | **PASS — winner non-LFM baru** | Produksi: merge di atas teks rapid intermediate `ocr_fallback.py` (zero extra latency, engine sudah jalan) = keputusan user terpisah; backend zero-touch |
| **HYB-004** | Angka resmi komposit all-74 utk HYB-003 (eval_hybrid_49 baru cover scan): hybrid rapid↔rapid_tess di 49 scan + baseline di 25 embedded, evaluasi in-memory pakai `merge_fields`/`evaluate_row` harness yg sama; artefak `ocr_experiment/composite_rapid_hybrid/eval_all74.json` | All-74 GT v9 + matcher v2: MACRO exact **50.32%** (fuzzy 63.87%) vs baseline resmi 47.26% (**+3.06pt**) — **gap ke komposit LFM (50.65%) tinggal 0.33pt @ 0 biaya**. Nomor komposit 61.5% (embedded menyeret naik), organizer 32.4%, dates 85.5% | **PASS** | Menjadi angka resmi non-LFM terbaik; pembanding wajib utk engine baru |
| **OCR-009** | Probe ppu-paddle-ocr (PP-OCRv6/v5 via ONNX standalone Bun binary) — TERKONFIRMASI beda dr OCR-001/002 (ONNX Runtime, bukan Paddle inference). 3 preset × 10 stem: v6-tiny, v6-small, v5-en-server; engine canvas-native; render page-per-page + **watchdog RSS** (SIGKILL process-group > cap). ⚠️ Run pertama TANPA guard memicu OOM/crash WSL user → hardening permanen: RLIMIT_AS TIDAK cocok utk runtime JS/WASM (reservasi virtual besar → false-kill); batas nyata = RSS polling /proc; peak RSS dicatat per panggung di meta.jsonl | Peak RSS nyata: v6-tiny ~1.3–1.7GB, v6-small ~1.6–2.4GB, **v5-en-server ~2.6–3.0GB (watchdog kill 10/10 → CLOSED ala OCR-001)**. Akurasi (subset-10): v6-tiny MACRO 40.0% (=baseline), nomor **14.3% vs baseline 28.6%** (FAIL digit — pola PP-OCR lagi), dates 88.9% (=), organizer fuzzy **80%** (tertinggi antar-engine!); v6-small FAIL (dates 66.7% regress, nomor 14.3%, MACRO 31.1%) | **v6-tiny/v6-small FAIL gate engine tunggal; v5-en-server CLOSED (RSS)** | Re-try: preset lain (v6-medium) butuh budget RSS >3GB — hanya dgn headroom host; ONNX CUDA EP saat jalur GPU siap; fine-tune dict/digit khusus |
| **HYB-005** | Pola HYB di atas temuan OCR-009 (organizer ppu-v6-tiny fuzzy 80% tertinggi): hybrid ppu-v6-tiny(organizer+dates)+baseline(nomor/role), subset 10-stem sama utk like-for-like vs HYB-003 | Subset-10: organizer exact **10% (1/10)** & fuzzy 80%; HYB-003 rapid pada subset sama: organizer exact **20% (2/10)** fuzzy 60%; MACRO exact **42.2% < 44.4% (HYB-003)**, biaya tambahan +~2.7s/cert (engine ekstra) tidak dibenarkan hasil | **FAIL/CLOSED — HYB-003 tetap winner non-LFM** | Hanya jika ada engine non-LFM yg kalahkan organizer-exact rapid (34.7% scan-49) tanpa regress digit |
| **OCR-010** | Probe GOT-OCR 2.0 (~580M VLM end-to-end, goal user non-LFM): bobot HF `stepfun-ai/GOT-OCR2_0` (~1.4GB) ke `~/.cache/got20/hf`; RTX 5050 GPU (torch 2.13+cu130 sudah ada di project env). Kompatibilitas: kode custom GOT utk transformers 4.37 → project env 5.14 gagal (`DynamicCache.seen_tokens`, SDPA mask shape) → solusi stack lama `pip --target ~/.cache/got20/legacy` (transformers 4.37.2 + tokenizers 0.15.2 + hub 0.19.4) di-PYTHONPATH-kan di depan worker (PYTHONPATH mendahului site-packages; torch project tetap terlihat). Worker subprocess load-sekali-per-chunk + watchdog RSS(6GB)+VRAM(7.6GB); inferensi fp16 `.to("cuda")` tanpa accelerate. `tests/benchmark_got_probe.py` + `tests/got_worker.py` | Probe 10 stem: **10/10 ok, ~16s/cert (load 8.5s sekali), peak RSS 2.3GB / VRAM 4.55GB — aman semua guard**. Kualitas teks VISUAL terbaik antar-engine (nomor benar, urutan semantik, nama+NIM penandatangan tertangkap), TAPI akurasi field: nomor **14.3%** vs baseline subset 28.6%, dates **77.8%** (<85.7 regress), organizer exact 10%/fuzzy 50%, MACRO single **35.6%**. Hybrid GOT(organizer+dates)+baseline: MACRO **42.2%** < HYB-003 44.4% pada subset sama. Insight: output VLM berbentuk prosa menghilangkan struktur baris yg dicari regex extractor — "OCR cantik" ≠ ekstraksi lebih baik | **FAIL gate engine tunggal; hybrid FAIL vs HYB-003 → CLOSED** | Re-try: `ocr_type='ocr-2'` (two-stage, biaya 2× inferensi); fine-tune/prompt format; ATAU ubah extractor agar agnostik struktur (riset C1 nama_kegiatan bisa menggabungkan ini) |
| **OCR-011** | Probe TrOCR (microsoft/trocr-base-printed, 333M encoder-decoder) + RapidOCR detection (PP-OCR ONNX). Bukan end-to-end: detect → crop → batch recognition. Butuh transformers 4.37 (5.14 gagal tokenizer) → legacy PYTHONPATH. `tests/benchmark_trocr_probe.py` + `tests/trocr_worker.py` | 10/10 ok, ~15s/cert GPU (load 12.7s sekali, inferensi 6-15s), peak RSS 2.5GB/VRAM 2.7GB. Deteksi bagus (13-32 regions, 0.3s). TAPI recognition **banyak typo** ("SERTIHKAT", "RAOTA", "DESELENGEARAKAN"), ALL-CAPS + format beda → **extractor 0/0 semua field**. Hybrid: MACRO 40.0% = baseline (0 gain) | **FAIL/CLOSED** | TrOCR-base-printed dilatih SROIE (resi Inggris) → tdk generalisasi font Indonesia. Fine-tune? Tapi deteksi tetap RapidOCR → gain minimal |
| **OCR-012** | Probe Keras-OCR (CRAFT+CRNN, ~120MB, TF-based). Venv terpisah `~/.cache/keras_ocr/venv` (TF 2.21 + keras-ocr 0.9.3) | **GAGAL SETUP** — keras-ocr 0.9.3 tdk kompatibel Keras 3.x (TF 2.21): `Dense(weights=...)` parameter dihapus. Error: `Unrecognized keyword arguments`. Project stale 2022 | **CLOSED (incompatible)** | Downgrade TF 2.15 + numpy<2 mungkin, tapi stack tua tdk layak |
| **C1-001** | Verifikasi frontier #5 handoff v38 ("nama_kegiatan 6.1% semua varian"): apakah extractor AKT-005 (`extract_activity_v5`) mentransfer ke teks OCR branch? `tests/verify_c1_akt_scan.py` (baru). Korpus `baseline_rapid_tess` (raw OCR: 49 scan + 25 embedded) — BUKAN `run_20260728_131835` (teks pipeline produksi; terbukti BEDA utk stem sama). GT v9 + matcher v2, 0 LLM | Scan-49: offline_prod nama_kegiatan exact **6.1%** (reproduksi persis klaim HYB-003 → artefak harness: eval cabang OCR memakai extractor PRODUKSI, AKT tak pernah dipasang di sana) → **+AKT-005: 59.2% (+53.1pt)**, fuzzy 12.2→83.7%, MACRO 59.60→70.00%; 27 fixes / 1 regress (KARSA_Venedict_peserta, suffix "(KARSA) 2023" terpotong) / 0 regress field lain. All-74 korpus ini: 8.1→56.8% | **PASS (verifikasi) — C1 RISET REDUNDAN dgn AKT-002..005**; "extractor agnostik struktur" (OCR-010) sudah ter-cover: anchor AKT pakai DOTALL lintas baris | Tidak perlu. Tindak lanjut: pasang AKT-005 di harness eval OCR / port produksi = keputusan user |
| **NC-004** | Re-try sempit NC-003 (frontier #2 v38): region **control rapid_tess multi-config** + anchor cache (`corpus_nomor_crop_rt6/anchors.json`, 32/49 hits) — `benchmark_nomor_crop.py run --region-engine rapid_tess --anchor-json ...`, run dir `corpus_nomor_crop_retry_ctrl_cache` | Nomor **57.6% (19/33) = baseline → gate ≥60.6% FAIL**. Recovery Falcon_Faiz `XI1`→`XII` TEREPRODUKSI (+1), TAPI voting-disagreement menimpa nomor benar IRIS (14): crop `2902/B/DST//UN3.FTMM/KM./2026` (double-slash) → −1 → net 0. Latency **avg 6.05s/cert (median 5.65) ≫ gate ≤2.0s FAIL** — region control sendiri 3.79s (stages: render 0.58 + anchor 1.74 + region_render 0.05 + region 3.79); proyeksi ~2.1s NC-003 salah (timing 0.55–1.64s itu engine murah, bukan control). 1 error render (Airno_Faiz malloc 234MB → fallback baseline) | **FAIL/CLOSED — NC-001 tetap OFF**; gain +3pt NC-001 rapuh (1 recovery, bergantung jalur kode/bbox), biaya produksi 2-pass +5.2s/cert tak berubah | Hanya jika ada region-OCR <1s yg pertahankan 60.6% (NC-002/003 semua FAIL akurasi) |
| **ROUTER-005** | Router expansion: mining 24 unrouted certs dengan sinyal organizer_v4 + activity_v5; 5-fold CV precision 100% gate. Rules: hima_pure_internal (+2), iris_ftmm_bso (+2), bem_ftmm_internal (+2) | Router coverage **50→56/74 (75.7%) @ 100.0% precision** (0 wrong across all 5 folds). LLM fallback calls 24→18 (-25%). | **PASS** — coverage +6 certs @ 100% precision, 0 false positive, min-fold 100% | Diadopsi di `backend/app/services/tingkat_router.py`. Sisa 18 certs genuine ambiguous (BEM context ganda, internasional, dll) tetap dioper ke LLM. |
| **COMBINED-V2-001** | Combined v2 Dummy Port: menyatukan AKT-005 (activity), ORG-004 (organizer), PROD-002 (nomor), ROUTER-005 (tingkat) di `app/services/combined_extractor.py` digate `ENABLE_COMBINED_V2=False` | Offline benchmark (74 certs, 0 LLM): nama_kegiatan 62.2%, organizer 63.5%, nomor 76.9%, tingkat 83.8%, **MACRO exact 74.2% (+18.5pt vs prod 55.7%)**, fuzzy 80.7%. QA fidelity 100% (0 mismatch vs ref). | **PASS** — staging bundle siap pakai, zero blast radius live | Flag default FALSE. Dapat diaktifkan via `ENABLE_COMBINED_V2=true` di `.env`. |
| **ROUTER-006** | Router contextual disambiguation (Branch 1): 7 rule kontekstual (aphsa_fkm, bem_nasional_act, kim_unair, dpkka_unair, intl_explicit, literasi_psikologi, ub_external_event) @ 5-fold CV precision 100.0% gate. | Router coverage **56→63/74 (85.1%) @ 100.0% precision** (0 false positive across all 5 folds). Unrouted residual certs **18→11 certs (-38.9%)**. | **PASS** — coverage +7 certs @ 100% precision | Bagian dari Combined v3 bundle. |
| **DATE-001** | Date extraction v2 (Branch 2): OCR year repair (2o24->2024), English ordinal stripping (23th, 7th-9th, 11th, 1lth), multi-day spans (month-first & day-first), single event date prioritization. | Waktu pelaksanaan exact **43→52/55 (94.5%, +16.4pt)**, 9 fixes / 0 regressions. | **PASS** — semua gate met, zero regression | Diadopsi di `backend/app/services/combined_extractor.py`. |
| **ORG-006** | Organizer normalization v6 (Branch 3): OCR spacing & acronym collapse ('Us U'->'USU'), HIMA S1->S-1, IRIS casing, date/faculty junk truncation, DPKKA/PCR/Telkom directorate fallback. | Penyelenggara exact **46→56/74 (75.7%, +13.5pt)**, fuzzy 81.1%, 10 fixes / 0 regressions. | **PASS** — gate >=73.0% PASS | Diadopsi di `backend/app/services/combined_extractor.py`. |
| **ACT-006** | Activity extraction v6 (Branch 4): OCR digit repairs (2 O 24->2024), AI vs Al fix, ANAVA/GRADIANT spacing repair, stop-token boundary preservation for English prepositions, trailing organizer stripping. | Nama kegiatan exact **46→56/74 (75.7%, +13.5pt)**, fuzzy 83.8%, 10 fixes / 0 regressions. | **PASS** — gate >=74.3% PASS | Diadopsi di `backend/app/services/combined_extractor.py`. |
| **NUM-003** | Certificate number v3 (Branch 5): Roman numeral month repair (XI1, XIl->XII, X1->XI) at /ROMAN/YEAR, DPKKA O0OO3 repair, Poisson dot-code format, Hitech dot-prefix recovery. | Nomor exact **40→46/52 (88.5%, +11.5pt)**, 6 fixes / 0 regressions. | **PASS** — gate >=86.5% PASS | Diadopsi di `backend/app/services/combined_extractor.py`. |
| **COMBINED-V3-001** | Combined v3 Composite Staging Bundle (Branch 6): Integrasi Branch 1-5 (ROUTER-006 + DATE-001 + ORG-006 + ACT-006 + NUM-003) di `app/services/combined_extractor.py` digate `ENABLE_COMBINED_V3=False` (default OFF). | **MACRO exact 74.2% → 85.7% (329/384, +11.5pt)**, fuzzy 88.3% (339/384). nama_kegiatan 75.7%, organizer 77.0%, nomor 88.5%, dates 94.5%, tingkat 89.2%. 0 LLM calls, 100% offline & deterministik. | **PASS — Milestone MACRO exact >= 85.0% tercapai (0 LLM, 0 blast radius)** | Flag default FALSE. Dapat diaktifkan via `ENABLE_COMBINED_V3=true` di `.env`. |
| **EXP-V4-001** | Combined v4 Composite Staging Bundle & Robust Acronym Metrology: (1) Matcher metrology upgrade in `tests/matchers.py` with multi-stage acronym engine (`is_initialism_of`, `is_portmanteau_of`, degree normalization `S1`<->`S-1`, strict negative discrimination), (2) ORG-007 organizer normalization, (3) `apply_combined_v4` in `app/services/combined_extractor.py` gated behind `ENABLE_COMBINED_V4=False` (default OFF, zero production blast radius). | **MACRO exact 75.0%**, fuzzy 77.3% (all-74 certs against GT v9). nama_kegiatan 75.7%, organizer 78.4%, nomor 63.9%, dates 71.2%, tingkat 89.2%. 0 LLM calls, 100% offline & deterministik. 5-fold CV 100.0% min-fold precision on router rules. pytest 82/82 passed. | **PASS** — zero regression, robust metrology, production safety preserved | Flag default FALSE. Dapat diaktifkan via `ENABLE_COMBINED_V4=true` di `.env`. |
| **EXP-V4-002** | Combined v4.1 Minor Staging Refinement (v4.x baseline comparison): (1) English ordinal date parser (`September 23th`), (2) Underscore nomor normalizer (`UNITY_UKMRT` <-> `UNITYUKMRT`), (3) Activity word-merge prepositions (`sebagaipeserta`, `SIDConnect`, title anchors), (4) Contextual Direktur Kemahasiswaan Universitas Airlangga router rule, (5) Gated behind `ENABLE_COMBINED_V4_1=False`. | **MACRO exact 75.68% → 76.82% (+1.14pt)**, fuzzy 77.95% → 78.64% (+0.68pt) vs v4.0 baseline. nama_kegiatan 75.7% → **79.7%** (+4.1pt), nomor 65.3% → **66.7%** (+1.4pt), tingkat 89.2% → **90.5%** (+1.4pt). 0 LLM calls, 100% offline & deterministik. pytest 88/88 passed. Zero regression on all fields. | **PASS** — Milestone v4.x improvement confirmed, production safety preserved | Flag default FALSE. Dapat diaktifkan via `ENABLE_COMBINED_V4_1=true` di `.env`. |
| **EXP-V4-003** | Combined v4.2 Staging Bundle (3 Pillars & High-DPI Robustness): (1) Pillar 1: Structural semantic grammar anchors in `extract_activity_v8`, (2) Pillar 2: Universal Roman numeral month repairs in `normalize_nomor_v5`, (3) Pillar 3: Calibrated confidence calculation (<0.85 review flag), (4) High-DPI region crop module in `high_dpi_crop.py` (6.0x zoom NC-001 pattern), (5) Gated behind `ENABLE_COMBINED_V4_2=False`. | **MACRO exact 76.82%** (all-440 cells), **87.42%** under official evaluation framework (271/310 non-empty cells). 0 LLM calls, 100% offline & deterministik. pytest 94/94 passed. OOD stress curves stable. Zero regression. | **PASS** — Milestone v4.2 integration complete, production safety preserved | Flag default FALSE. Dapat diaktifkan via `ENABLE_COMBINED_V4_2=true` di `.env`. |
| **B1-HIGH-DPI-001** | Fast semantic anchor (re-try NC-003/004): text-search PyMuPDF dulu, fallback RapidOCR @1.5x utk anchor high-DPI crop; benchmark 49 scan, GT v9 + matcher v2, eval resmi harness (`tests/benchmark_b1_high_dpi.py`) | baseline 60.6% (20/33) | control NC-001 63.6% (21/33) | **B1 54.5% (18/33) REGRESS di bawah baseline**; b1_psm6 57.6%; b1_module (produksi high_dpi_crop) 60.6% (=baseline, 0 hit di scan). Anchor B1 3.05s/cert vs control 3.54s — downscale 1.5x TIDAK memangkas RapidOCR (4.3–6.7s kedua zoom; biaya det+rec). Text-search 0% berguna di 49 scan (tanpa text layer; y-posisi nomor 0.36–0.91 page → tanpa shortcut strip). Latency tambahan 6.72s/cert (region multi-config 3.57s) > gate 1.8s. Zero regression dates/org | **FAIL semua gate** — anchor 1.5x menurunkan kualitas crop (voting-disagreement menimpa nomor benar); floor latency = full-page OCR tak bisa <0.5s | Anchor = reuse word-boxes dari primary OCR pass (0 OCR tambahan) + region engine lebih cepat dari multi-config tanpa kehilangan akurasi (keputusan produksi, bukan eksperimen) |
| **B2-OOD-001** | OOD probe langsung `apply_combined_v4_2` (`tests/ood_probe_v4_2.py`): mutation UNAIR→UNS/FTMM→FST/event generik + noise 5↔S/8↔B/0↔O/1↔I 10/25/50%, seed 42, input identik v9 & v4.2 | Baseline v4.2 MACRO 88.0% (384 sel) vs v9 56.5%. Mutation: free-inst drop v4.2 **+7.4pt > gate 2.0pt FAIL** — 16/21 sel hilang = kontaminasi GT (nilai ikut institusi baru, GT lama statis), kerapuhan nyata 5 sel ≈1.9pt. Noise 10% PASS (drop 8.1 vs 8.6 v9); 25% FAIL (+18.2 vs +16.7; extra = nomor 26vs16, kegiatan 5vs1); 50% FAIL (+27.9 vs +24.2; extra = kegiatan 17vs3, nomor 40vs27); v4.2 lebih tahan noise di tanggal (5vs10 / 24vs35) | **FAIL raw gate** — kerapuhan terkonsentrasi di literal event ber-token institusi (KARSA FTMM/SPECTA/Airlangga) + Roman-repair pada digit noise | Re-run setelah de-corpusing (B7) + guard Roman (B6): composite B8 OOD = identik v4.2 (0 perubahan nilai → kerapuhan tetap) |
| **B3-CONF-001** | Kalibrasi confidence per pola utk safety net v4.2 (`tests/calibrated_confidence_v4_2.py`): kegiatan PKKMB 0.95/struktural 0.85/quoted 0.82/hardcoded 0.55/base 0.60; nomor strict 0.95/repaired 0.78; dates interval 0.95/pair 0.88/single 0.75; tingkat base 0.98/disambig 0.84 — flag via `field_needs_review` (<0.80) | N=74 GT v9: cert recall **100%** (gate ≥95 PASS; statis v4.2 64.7%), weak-field kegiatan 93.3% (PASS) tapi **nomor 25%** (1/4 — error digit tak terlihat pola string, gate ≥88 FAIL), false alarm **90%** (36/40 cert perfect ter-flag — literal 0.55/repaired 0.78/single-date 0.75 by design, gate ≤20 FAIL) | **FAIL** — gate plan saling bertentangan (0.55 utk literal menjamin false alarm tinggi; nomor butuh sinyal OCR/region-disagreement) | Confidence berbasis OCR/multi-engine disagreement + threshold precision-aware; B3 tidak masuk composite (tidak mengubah nilai — hanya flag) |
| **B4-ROUTER-001** | Hardening 8 rule disambig (`tests/router_disambig_v7.py`): kim_unair `\bKIM\b`+tolak KIMIA; intl_explicit guard FACULTY OF/DEPARTMENT OF/FAKULTAS + institusi asing eksplisit; ub_external_event `\bUB\b|BRAWIJAYA|FILKOM`; bem_nasional_act "HARI ANAK NASIONAL" utuh; direktur_kemahasiswaan_unair formal rule #8 | 18 unit adversarial PASS (Olimpiade KIMIA / Robotics Club / Faculty of Informatics Engineering / BEM FKM Hari Anak Kampus). 5-fold CV (SEED 42, `stat_validation_disambig.py`): coverage **64/74** (gate ≥63 PASS), min-fold precision **100%** semua rule firing; 7/8 rule LOW-N (fire <5, dilaporkan per Assumption 2) | **PASS** — 0 false positive di holdout, coverage naik; 0 perubahan nilai corpus (protective) | Diadopsi ke produksi menunggu keputusan user; perlu re-CV bila korpus bertambah |
| **B5-ACT-001** | Structural anchors v9 (`tests/activity_extractor_v9.py`): closers ID/EN diperluas (diadakan oleh/yang diadakan/bekerja sama dengan/bertempat di/hosted by/presented by/in cooperation with/in collaboration with), fallback `$` dihapus (structural capture ≤120 char/18 kata), sanitasi petik tak sepasang + newline→spasi, anti-bleed cut tier v7 (closer masa bakti/tahun dikecualikan — v6 sengaja mempertahankan) | 11 unit robustness PASS (0 organizer bleed). Benchmark 74: exact **79.7%** (=v4.2, zero exact-flag regress), fuzzy **85.1%** (gate 83.8 PASS), 1 benign value change (newline→spasi, Ananda) | **PASS** — protective; korpus tidak mengeksekusi kasus bleed (kasus audit QA = OOD/sintetik) | Diadopsi utk composite B8; korpus baru dgn variasi "diadakan oleh" akan menguji fix |
| **B6-NUM-001** | Nomor v6 (`tests/nomor_normalizer_v6.py`): DPKKA cleaner length-preserving (`replace("O","0")` — "OOO4"→"0004" BUKAN "00004"), prefix NOMOR/NUMBER didukung, guard Roman (segmen pure-digit "1" TIDAK di-overcorrect → flag 0.78; XI1/X1 mixed tetap di-repair; DPKKA-ctx "1"→"I" karena prefix O-run bukti OCR korup), month class DPKKA diperluas VXLCDM | 13 unit PASS (invariant panjang digit). Benchmark 74: exact **92.3%** (48/52, =v4.2), fix 0 regress 0 — 2160238 DPKKA.S "1"→"I" dipertahankan via dpkka_ctx | **PASS** — preservasi digit terjaga, zero regression | Diadopsi utk composite B8; overcorrection "1"→"I" produksi (v3/v5) menunggu keputusan promosi |
| **B7-DECORP-001** | Audit de-corpusing (`tests/decorpusing_audit.py`): 18 literal ter-memorasi (kegiatan 11, nomor 2, organizer 5) + ablasi Mode A (assisted, 88.0%) vs Mode B pure semantic (**79.4%**) — assist total +8.6pt (kegiatan +12.2, organizer +31.1, nomor 0.0, tingkat +1.4) | Katalog lengkap di `docs/report/decorpusing_catalog.md` (literal, file:fn, field, firing freq, rekomendasi). Nomor literal 270/GIRI assist 0 → dapat dilepas GRATIS. Organizer alias (F1/R6/FORKAS/himasta) = 31pt — kontribusi terbesar, generalisasi semantic diperlukan sebelum de-corpusing produksi | **PASS** — 100% literal terinventarisasi + baseline Pure Semantic terbentuk | Pure Semantic = acuan OOD objektif; de-corpusing produksi menunggu pengganti semantic utk organizer |
| **B8-COMPOSITE-001** | Composite candidate (`tests/composite_v4_candidate.py`) = v4.2 + B4 router v7 + B5 activity v9 + B6 nomor v6 (B1/B3 FAIL → excluded; HYB-003 report-only korpus OCR). Benchmark 3 arah 74 teks, GT v9 + matcher v2 | v9 46.17% | v4.2 **76.13%** (basis 444 sel; ledger 76.82%/440 — drift matcher-basis) | composite **76.13%** all-cells, framework 87.42% (=v4.2 persis ledger). Zero regression per field PASS. OOD composite = identik v4.2 (mut free-inst +7.4pt, noise 10% +7.6/25% +12.2/50% +27.9). **Gate akurasi FAIL**: 76.13 < 78.0 & 87.42 < 89.0 — komponen B4-B6 protective (0 perubahan nilai; kasus audit QA = OOD/sintetik, bukan corpus) | **FAIL gate akurasi** / PASS zero-regression + 4 lapis bukti lengkap (CV B4 100%, OOD curve, katalog B7, review B3) | Korpus dgn variasi template nyata (bleed "diadakan oleh", DPKKA O-run, bulan "1") atau integrasi OCR-crop dgn anchor reuse primary pass |
| **OCR-TESS-V4-001** | Tesseract-Primary standalone OCR (multi-PSM: `""`, `--psm 6`, `--psm 11`) dipadukan suite post-processing Composite v4.x (B8: activity v9, nomor v6, organizer v7, date v2, router v7). 49 scan + 25 embedded, GT v9 + Matcher v2. Evaluasi metrik ganda (5-field framework 310 sel & 6-field all-cells 444 sel). Runner: `tests/benchmark_tesseract_v4.py` | Scan-49 framework exact **78.61%** (fuzzy **86.57%**), melompat **+31.35pt** vs baseline rapid_tess (47.26%). Scan nomor exact **87.88%** (29/33, +30.3pt / +10 recover); kegiatan exact **73.47%** (36/49, +67.35pt / +33 recover); organizer exact **51.02%** (25/49, +24.49pt / +12 recover); dates exact **97.14%** (34/35). All-74 framework exact **77.10%** (fuzzy 83.55%). All-cells 6-field: Scan-49 **67.69%**, All-74 **68.02%**. Latensi scan avg **4.74s** (med 3.72s) — **45.0% lebih cepat** dari rapid_tess (8.61s). 0 crash, 74/74 valid. | **PASS** — Semua gate tercapai, performa scan melonjak drastis vs baseline OCR lama, membuktikan Tesseract-primary viable dan optimal untuk CPU deployment. | Disimpan sebagai baseline OCR primer baru di `tests/benchmark_runs/ocr_experiment/tesseract_primary_v4/`. |
| **OCR-TESS-PURE-001** | Pure 100% Tesseract OCR pada seluruh 74 sertifikat (25 digital PDF dipaksa render PNG @3.0x & dibaca Tesseract multi-PSM dari nol, tanpa layer teks digital sama sekali) + Composite v4.x suite. GT v9 + Matcher v2. Runner: `tests/benchmark_tesseract_v4.py --force-ocr-all` | Full 74 certs 100% OCR: framework MACRO exact **77.10%** (identik mode hybrid 77.10%), fuzzy **85.16% (+1.61pt)**; All-cells 6-field **68.47% (+0.45pt)**. Pada 25 PDF digital murni: exact **74.31%** (zero degradation!), fuzzy **82.57% (+4.59pt)**; nomor exact **89.47% (17/19, zero loss)**; kegiatan exact **76.00% (+8.0pt)**; dates exact **90.00% (+10.0pt)**; organizer 36.00% (-24pt). 0 crash, 74/74 valid. | **PASS** — Membuktikan Tesseract tangguh membaca gambar murni dari nol; rekonstruksi layout Tesseract justru menyatukan teks kegiatan & tanggal yang terfragmentasi pada PDF digital. | Disimpan di `tests/benchmark_runs/ocr_experiment/tesseract_pure_all74_v4/` & `docs/report/tesseract_pure_all74_v4_report.md`. |
| **ORG-TESS-V8-001** | Tesseract raw OCR organizer v8: structural phrase/date bounds, conservative signer-column recovery, number-line guard, explicit organization aliases; staging-only via `--use-v8-organizer`, raw OCR reused with **0 extra OCR calls**. GT v9 + matcher v2; Pure All-74 and Primary Hybrid All-74. | Pure organizer **34→39/74 (+5)**; Primary **40→44/74 (+4)**; Primary Scan-49 **25→29/49 (+4)**. Number/dates unchanged; row-level number/date regressions **0**; scan framework **78.61→80.60%**. 5-fold source precision: phrase 77.78% pure / 83.33% primary, signer 51.43% / 52.17%, signer_join 100% but LOW-N=1. Bootstrap organizer delta +6.76pt pure (95% CI −6.76..18.92), +5.41pt primary (−5.41..16.22). OOD entity mutation 36.49%/40.54%; OCR noise 10/25/50% = 25.68/18.92/12.16% pure and 24.32/17.57/12.16% primary. Safety review recall at protocol threshold 0.85 = 88.57%/76.67%; runtime threshold 0.80 = 5.71%/13.33%. Reports: `docs/report/tesseract_pure_all74_org_tess_v8_report.md`, `docs/report/tesseract_primary_v4_org_tess_v8_report.md`, `docs/report/org_tess_v8_validation.md`. | **FAIL** — organizer gates miss targets 45/74 (pure) and 50/74 (primary); CV min-fold precision <100% and safety recall <95%; no production promotion. | Redesign candidate arbitration and confidence/review routing; rerun all four proof layers before any retry or production discussion. |
| **EXP-LLM-002** | Direct Tesseract-to-LLM comparative extraction benchmark (0 regex/rule extraction) menggunakan 3 model Google Gemini (`gemini-2.5-flash`, `gemini-2.5-flash-lite`, `gemini-3.1-flash-lite`) pada 74 teks korpus Tesseract-Primary vs GT v9 + Matcher v2. Endpoint REST standard library + deterministic generationConfig (`temp=0.0`, `json_mime=True`), mandatory full_text injection ke form_mapper, pencatatan token & tarif resmi USD/IDR (kurs Rp17.758). Runner: `tests/benchmark_gemini_tesseract.py` & `tests/validate_gemini_4layer.py` | **Winner: gemini-3.1-flash-lite** — MACRO exact **63.74%** (fuzzy 72.75%), Framework exact **63.24%** (fuzzy 73.78%), Scan-49 exact **64.97%**, Emb-25 exact **61.33%**; per-field: kegiatan 62.16%, nomor 59.46%, org 59.46%, tanggal 67.57%, tingkat 66.22%. Mengalahkan baseline LLM v9 lama (60.2% MACRO exact). Total biaya 74 certs **Rp703.27 (~Rp9.50/cert)**, avg token 1,369 tok/cert, avg latensi **1.36s/cert**. Model lain: 2.5-flash 58.56% (Rp38.94/cert, 4.62s), 2.5-flash-lite 58.78% (Rp3.26/cert, 1.32s). 4-Lapis: CV 5-fold 63.79% (std 6.70%), Bootstrap 95% CI [58.78%, 68.92%], OOD noise 50% tahan 58.89%, anti-hardcode 0 pelanggaran, review recall 55.88% @ 100% precision. | **PASS** — direct LLM murni mengalahkan baseline v9 lama dengan biaya sangat murah (<Rp10/cert) dan kecepatan tinggi (1.36s). | Opsi staging jika tim ingin pipeline direct LLM tanpa pemeliharaan regex kompleks; untuk mencapai level Combined v4.2 (76.8%), direct LLM dapat dipadukan dengan router tingkat atau anchor crop nomor. |
| **PROD-GEMINI-001** | Production promotion Option A (Direct Tesseract-to-Gemini Replacement): `backend/app/services/gemini_extractor.py` + toggle `ENABLE_TESSERACT_GEMINI` (default true per user approval) di `config.py` & `extraction_pipeline.py`. Menggantikan ekstraksi regex/rule lama dengan direct call ke `gemini-3.1-flash-lite` saat flag ON, dengan graceful fallback otomatis ke pipeline offline jika API key hilang, kuota habis, atau terjadi network timeout (zero 500 errors). | **PROMOTION SUCCESS** — 163 pytest passing (100%), live smoke test `run_extraction_pipeline` pada PDF riil berhasil mengekstrak dan memetakan seluruh field form KHP via `gemini-3.1-flash-lite` dengan engine `pymupdf_fast_path+gemini-3.1-flash-lite`. Graceful fallback terverifikasi saat Gemini disimulasikan error. Docker tidak dinyalakan sesuai instruksi user. | **PASS** — Option A live in backend, feature toggle ON by default, zero blast radius if switched OFF. | Diuji langsung di backend; jika tim ingin kembali ke rule offline, cukup set `ENABLE_TESSERACT_GEMINI=false` di `.env`. |
| **NER-ENCODER-001** | Comparative 5-fold stratified cross-validation fine-tuning multilingual token classification encoders (`microsoft/mdeberta-v3-base` 86M & `facebookai/xlm-roberta-large` 560M) pada 74 teks korpus OCR Tesseract + BIO silver-labeling vs GT v9 + Matcher v2 (310 sel framework). Loss terbobot square-root inverse-frequency, Adafactor optimizer, gradient checkpointing, FP32 weight stabilization, dan evaluasi 4-lapis (5-fold CV, Bootstrap 1000x CI, OOD mutasi & noise OCR). Runner: `tests/benchmark_ner_encoders.py` | Sanity overfit 100% recall non-O (25/25 tokens). **mdeberta-v3-base**: Framework exact **34.84%** (108/310), fuzzy **51.29%** (159/310), 95% CI [28.95%–40.92%]; kegiatan 25.68% (fuzzy 70.27%), nomor 55.77%, org 25.68%, tgl mulai 32.73%, tgl selesai 41.82%; OOD mut drop 7.20pt, noise-10% drop 12.71pt. **xlm-roberta-large**: Framework exact **43.55%** (135/310), fuzzy **55.48%** (172/310), 95% CI [37.17%–50.00%]; kegiatan 35.14% (fuzzy 70.27%), nomor 59.62%, org 37.84%, tgl mulai 45.45%, tgl selesai 45.45%; OOD mut drop 7.63pt, noise-10% drop 16.10pt. Melompat **+30.75pt** vs baseline NER v1 (12.8%). | **PASS (Benchmark OOF)** — Signifikan melampaui NER v1 (12.8% → 43.55%). **TIDAK DIPROMOSIKAN KE PRODUKSI** karena performa exact masih di bawah Composite v4.x (77.10%) dan Gemini-3.1-flash-lite (63.24%). | Ditutup sebagai referensi arsitektur encoder lokal; re-try hanya layak bila tersedia dataset beranotasi gold manual ≥500 dokumen. |
| **NER-ENCODER-002** | Fine-tuning ulang model monolingual Indonesia `treamyracle/indobert-ner-gold` (334M) dengan konfigurasi yang disetarakan penuh vs mDeBERTa & XLM-RoBERTa (5-fold stratified cross-validation, out-of-fold evaluation, weighted loss square-root inverse-frequency, Adafactor optimizer, effective batch size 8, 10 epoch, Fast Tokenizer sliding window stride 64). GT v9 + Matcher v2 pada 74 teks Tesseract OCR Standalone (310 sel framework). Runner: `tests/benchmark_ner_encoders.py` | Sanity overfit 96.0% recall non-O (24/25 tokens). Framework MACRO exact **44.52%** (138/310), fuzzy **57.10%** (177/310), melonjak **+31.72pt** vs baseline pre-trained v1 (12.8%) dan membalikkan kegagalan v2 (1.5%). Bootstrap 95% CI: **[38.59%, 50.82%]**, mean 44.57%. Per-field: **nama_kegiatan 43.24%** (fuzzy **78.38%** — tertinggi di antara seluruh encoder, melampaui DeBERTa 25.68% & RoBERTa 35.14%); nomor 57.69% (fuzzy 57.69%); penyelenggara 36.49% (fuzzy 54.05%); tgl mulai 49.09%; tgl selesai 40.00%. OOD: Clean 47.03%, mutasi entitas 39.83% (-7.20pt), noise 10% 30.51% (-16.53pt), noise 25% 18.22% (-28.81pt), noise 50% 8.90% (-38.14pt). Training 5 fold: 700.1s (~11.7m), peak VRAM 4.65 GB. | **PASS (NEW BEST ENCODER)** — Membuktikan hipotesis user benar: dengan konfigurasi loss terbobot dan sliding window yang tepat, checkpoint IndoBERT yang kaya representasi entitas Indonesia mampu mengungguli model multilingual yang 1.7x lebih besar (XLM-RoBERTa 560M @43.55%). Tetap tidak dipromosikan ke produksi karena masih di bawah Composite v4.x (77.10%) dan Gemini direct (63.24%). |
| **NER-GLINER-001** | Zero-shot open-type information extraction benchmark menggunakan GLiNER v2.1 Multilingual (`urchade/gliner_multi-v2.1` span architecture) dan GLiNER2.5 Base (`fastino/gliner2.5-base-v1` boundary architecture dari paper arXiv:2507.18546) pada 74 teks korpus Tesseract OCR vs GT v9 + Matcher v2 (310 sel framework). Query skema dinamis (event name, certificate number, organizer, date). Runner: `tests/benchmark_gliner.py` | **gliner-multi-v2.1**: Framework MACRO exact **35.48%** (110/310), fuzzy **46.45%** (144/310), Bootstrap 95% CI [30.10%–41.31%]; tgl mulai/selesai exact 58.18%, organizer 36.49%, nomor 13.46%, kegiatan 16.22%; **OOD mutasi entitas drop 0.00pt (kebal mutasi!)**; latensi 0.077s/cert (5.7s total 74 cert). Mengungguli fine-tuned mDeBERTa-v3-base (34.84%) secara murni zero-shot! **gliner2.5-base**: Framework exact **28.71%** (89/310), fuzzy **38.71%**, Bootstrap 95% CI [23.64%–34.39%]; **nama kegiatan exact 31.08%** (fuzzy 52.70%, mengungguli DeBERTa FT 25.68%); mutasi drop 0.42pt; latensi 0.052s/cert (3.9s total). | **PASS (Benchmark Zero-Shot)** — Membuktikan arsitektur schema-driven GLiNER sangat tangguh terhadap mutasi entitas OOD (zero degradation) dan memiliki kecepatan inferensi tinggi (3.9–5.7s untuk seluruh dataset). Namun akurasi keseluruhan (28.7%–35.5%) masih di bawah encoder terlatih domain IndoBERT (44.52%), Gemini (63.24%), dan Composite v4.x (77.10%). |
| **NER-GLINER-002** | Fine-tune OOF GLiNER v2.1 / GLiNER2.5 dengan split encoder identik, label whole-token konservatif (130 tuple), window tokenizer-aware 512/64, subprocess per fold (isolasi memori WSL), dan evaluasi OOF 5-fold vs GT v9 + Matcher v2 (310 sel framework). | **gliner-multi-v2.1**: Framework MACRO exact **54.52%** (169/310), fuzzy **63.23%** (196/310), melonjak **+19.04pt** vs zero-shot v2.1 (35.48%) dan **+10.00pt** vs IndoBERT-ner-gold (44.52%), menjadi encoder tertinggi. Per-field: kegiatan 52.70%, nomor 55.77%, penyelenggara 50.00%, tgl mulai 52.73%, tgl selesai 63.64%. Bootstrap 95% CI: [47.78%, 60.76%]. Waktu: 649.7s (~10.8m), VRAM 3.76 GB. **OOD Robustness (Lapis 2) FAIL**: Clean 55.93%, Mutation 49.58% (drop -6.36pp > gate 2.0pp), Noise 10% 36.86% (drop -19.07pp), Noise 25% 27.54% (-28.39pp), Noise 50% 13.56% (-42.37pp). Raw confidence uncalibrated (avg 1.02 > 1.0). **gliner2.5-base**: **FAIL (FloatingPointError micro-batch non-finite loss)** di Fold 1 epoch 4 pada native AdamW. | **FAIL (Robustness & Numerik)** — v2.1 OOF selesai (54.52%) tapi gagal Lapis 2 OOD mutasi (-6.36pp) & sanity recall 75%; GLiNER2.5 divergen numerik. Zero blast radius produksi. | Tidak layak promosi produksi (Option A Gemini tetap unggul 63.24% deterministik). Retry GLiNER2.5 hanya jika boundary loss di-clamp/clip khusus. |
| **PROD-INPUT-MATRIX-001** | Evaluasi komparatif fresh 6 varian pembentukan teks input (production_conditional, pymupdf_only, rapidocr_only, tesseract_only, rapid_tesseract_only, always_hybrid) pada 74 sertifikat sumber asli (PDF & scan raster) via Gemini 3.1 Flash Lite pasca-penghapusan Docling. GT v9 + Matcher v2 frozen. Runner: `tests/benchmark_production_input_matrix.py` | Baseline control (production_conditional): 67.57% all-cells / 81.29% framework | **Winner: production_conditional** — All-Cells MACRO exact **67.57%** (fuzzy **75.68%**), Framework exact **81.29%**, Scan-49 exact **65.31%**, Emb-25 exact **72.00%**, missing values 48, 95% CI [62.39%, 72.30%]. Varian murni OCR: rapid_tess **66.89%** (Emb-25 drop ke 68.0%), tesseract_only **65.77%** (Emb-25 drop ke 68.0%), always_hybrid **66.67%** (Emb-25 drop ke 70.0%), rapidocr_only **63.06%**, pymupdf_only **27.70%** (scan collapse 9.18%). | **PASS (Production Winner Confirmed)** — Membuktikan secara empiris bahwa conditional routing produksi (PyMuPDF fast-path untuk PDF digital murni + RapidOCR/Tesseract fallback saat scan/missing dates) mengungguli full OCR murni karena mencegah degradasi derau OCR pada 25 PDF digital (+4.00pt pada Emb-25). | Menjaga arsitektur production conditional sebagai standar rujukan produksi; varian full Tesseract tetap disimpan sebagai alternatif andal bila layer teks PDF digital rusak. |
| **PROD-EPHEMERAL-001** | Arsitektur Ephemeral Zero-PDF Storage di PostgreSQL: penghapusan penyimpanan BYTEA (`document_files`) dan raw OCR (`parsed_documents`), pengalihan file sementara ke `TemporaryUploadStore` (`UPLOAD_TEMP_DIR` / 0o600 / UUID validation), pembersihan otomatis seketika (`unlink`) di blok `finally` saat job berstatus terminal, serta isolasi kebocoran file. Runner: `tests/test_ephemeral_storage.py` + full pytest. | **GATE PASS** — 100% PDF bytes dieliminasi dari database PostgreSQL (0 byte bloat); 100% file sementara di-unlink otomatis pasca-ekstraksi terminal; pencegahan path traversal & non-PDF magic bytes terverifikasi; 183/183 unit tests passing (100%). | **PASS — Arsitektur Zero-PDF Ephemeral Storage Berhasil Diimplementasikan** | Siap dideploy ke staging/produksi; retensi form KHP dan metadata job berumur 24 jam sementara file fisik langsung musnah seketika. |
| **PROD-AUTORETRIEVE-001** | SHA-256 Deduplikasi & Client Session Auto-Retrieve: Cek hash `checksum_sha256` pada endpoint upload (`POST /api/documents`) untuk file yang telah berstatus `completed`/`needs_review` di PostgreSQL, mengembalikan hasil instan (<10ms) tanpa panggil Gemini/OCR ulang, dipadukan persistensi sesi `localStorage` (`cert_last_document_id`) di frontend agar refresh browser (F5) memulihkan form terisi otomatis. Runner: `tests/test_ephemeral_storage.py::TestUploadDeduplicationAutoRetrieval`. | **GATE PASS** — Upload PDF identik mengembalikan `job_id="cached"` dengan ID dokumen yang sama; 0 ekstraksi ulang; 0 konsumsi token Gemini; pemulihan sesi frontend terverifikasi; 184/184 unit tests passing (100%). | **PASS — Auto-Retrieve & Deduplikasi Berhasil Diimplementasikan** | Menghemat token & waktu saat file diunggah ulang; user experience refresh browser terjaga. |
| **PROD-PREVIEW-001** | Option 1 Ephemeral + Compressed JPEG Preview: Generasi thumbnail halaman 1 via PyMuPDF JPEG encoder @110 DPI (downsampling fallback @72/50 DPI bila >150 KB) disimpan pada `documents.preview_image` dan diekspos via endpoint `GET /api/documents/{id}/preview`, dipadukan lifecycle cleanup cascade 24 jam berbasis `ExtractionJob.finished_at` pada `retention_cleanup.py` (dieksekusi periodik di `db_worker` dan startup). Catatan batasan: preview tanpa otorisasi per-user dan background mode tidak menjalankan periodic loop. Runner: `tests/test_ephemeral_storage.py::TestCompressedPreview`. | **GATE PASS** — Sampel fixture 34 KB (<150 KB cap); kegagalan render terisolasi (returns None, ekstraksi tetap berjalan); endpoint preview mengembalikan image/jpeg; cleanup cascade terverifikasi; 186/186 unit tests passing (100%). | **STAGING/CONDITIONAL PASS — Option 1 (Ephemeral + Compressed JPEG Preview) Berhasil Diimplementasikan** | Solusi seimbang: preview tetap tersedia saat refresh dan verifikasi, sementara storage database terkelola dengan batas ukuran dan TTL 24 jam. Promosi produksi menunggu otentikasi/RBAC endpoint preview. |
| **PROD-AUTORETRIEVE-002** | Refinement Auto-Retrieve & Eliminasi Zombie LocalStorage: (1) Menghapus auto-restore dari `localStorage` agar form selalu bersih saat user membuka halaman dari awal atau melakukan refresh biasa (mencegah salah sangka reset vs new session), (2) Mendukung pemulihan dokumen secara eksplisit dan terkontrol via URL query parameter (`?doc=<id>`), (3) Backfill otomatis thumbnail `preview_image` saat PDF identik diupload ulang jika record lama di database belum memiliki preview, (4) Menjaga preview PDF resolusi asli (`state.objectUrl`) saat upload baru tanpa tertimpa thumbnail server. Runner: `tests/test_ephemeral_storage.py` + browser UI verification. | **GATE PASS** — Halaman baru bersih 100% (form kosong, preview hidden, 0 zombie localStorage); URL param `?doc=` memulihkan dokumen & preview dengan tepat; tombol RESET membersihkan form & query param URL; backfill preview pada re-upload terverifikasi; 186/186 pytest passing (100%). | **PASS — UX Pemulihan Sesi Bersih & Zero Zombie Caching** | Solusi atas issue caching frontend tanpa mengorbankan backend SHA-256 deduplication. |

---

## Hubungan dengan dokumen lain

- `handoff_v49.md` = **open frontier** (sanity GLiNER FAIL; OOF diblokir).
- `runs_summary.md` = **angka terukur** (semua run).
- `experiments_ledger.md` (ini) = **closed list** (yang sudah dicoba & ditutup).
